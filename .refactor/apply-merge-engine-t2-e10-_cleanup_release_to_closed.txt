extraction e10 on HEAD 4c47ab8f4082db15bc89e5af9cd45cea42572805

== dry run
$ python3 /home/agents/foundry-of-zero/refactor-python/skills/decompose/scripts/extract_ranges.py --project . --source scripts/forge/forge_cli/app/_engine_cleanup.py --function cleanup_chain --start 443 --end 460 --name _cleanup_release_to_closed --import-root scripts/forge --format-imports --manifest .refactor/merge-engine-t2-e10-_cleanup_release_to_closed.json
--- scripts/forge/forge_cli/app/_engine_cleanup.py
+++ scripts/forge/forge_cli/app/_engine_cleanup.py
@@ -440,26 +440,30 @@
                         "forge: merge cleanup failed — branch-delete did not PASS",
                     )
 
-                with self.store.event_lock(str(current["chain_id"])):
-                    cleanup_replay = self.store._read_replay_locked(
-                        str(current["chain_id"])
-                    )
-                summary = chain_core._merge_cleanup_history_summary(
-                    cleanup_replay.events
-                )
-                if not (
-                    summary.get("remote_containment") is not None
-                    and summary.get("worktree_complete") is True
-                    and summary.get("branch_complete") is True
-                ):
-                    raise FrozenError(
-                        "cleanup did not durably complete every required step",
-                        chain_id=str(current["chain_id"]),
-                        schema=REVISION9_OUTPUT_SCHEMA,
-                    )
-                current = self._release_to_closed_locked(current, lease)
+                current = _cleanup_release_to_closed(self, current, lease)
     return engine._success(
         current,
         f"merge chain {current['chain_id']} cleanup is durably closed",
         "none — merge chain closed",
     )
+
+def _cleanup_release_to_closed(self, current, lease):
+    with self.store.event_lock(str(current["chain_id"])):
+        cleanup_replay = self.store._read_replay_locked(
+            str(current["chain_id"])
+        )
+    summary = chain_core._merge_cleanup_history_summary(
+        cleanup_replay.events
+    )
+    if not (
+        summary.get("remote_containment") is not None
+        and summary.get("worktree_complete") is True
+        and summary.get("branch_complete") is True
+    ):
+        raise FrozenError(
+            "cleanup did not durably complete every required step",
+            chain_id=str(current["chain_id"]),
+            schema=REVISION9_OUTPUT_SCHEMA,
+        )
+    current = self._release_to_closed_locked(current, lease)
+    return current
{
  "version": 1,
  "tier": 2,
  "baseline": {
    "scripts/forge/forge_cli/app/_engine_cleanup.py": "652327739a2083805f1fadb3282b8d80fa35d342012953e3e17a17f08f67f5dd"
  },
  "operations": [
    {
      "kind": "extract",
      "source": "scripts/forge/forge_cli/app/_engine_cleanup.py",
      "dest": "scripts/forge/forge_cli/app/_engine_cleanup.py",
      "function": "cleanup_chain",
      "name": "_cleanup_release_to_closed",
      "imports": {},
      "start_line": 443,
      "end_line": 460,
      "parameters": [
        "self",
        "current",
        "lease"
      ],
      "outputs": [
        "current"
      ],
      "format_imports": true
    }
  ]
}
DRY RUN: verified; no files written
[exit 0]

== snapshot
$ python3 /home/agents/foundry-of-zero/refactor-python/skills/split-module/scripts/snapshot_bodies.py snapshot scripts/forge/forge_cli --out .refactor/merge-engine-t2-e10-before.json
snapshot: 890 bodies across 876 names -> .refactor/merge-engine-t2-e10-before.json
[exit 0]

== apply
$ python3 /home/agents/foundry-of-zero/refactor-python/skills/decompose/scripts/extract_ranges.py --project . --source scripts/forge/forge_cli/app/_engine_cleanup.py --function cleanup_chain --start 443 --end 460 --name _cleanup_release_to_closed --import-root scripts/forge --format-imports --manifest .refactor/merge-engine-t2-e10-_cleanup_release_to_closed.json --apply
APPLIED tier 2; manifest: .refactor/merge-engine-t2-e10-_cleanup_release_to_closed.json
[exit 0]

