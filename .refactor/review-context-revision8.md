# Reviewer context for the revision8 test split (branch refactor/split-test-revision8-coordination, BASE 69bc28d, reviewed tip = finalize commit 97d4d60)

Session: bead forge-plugin-25ms, lane B1 of docs/analysis/refactor-overnight-brief-2026-09-23.md, plugin refactor-python 0.1.3. Test-only decompose: mixin shape for the 31 helpers (tests/_revision8_support.py, commit a1defe4, manifest .refactor/mixin-revision8-mixin.json, --test-mode identity) and class shape for the 81 tests in seven families (commits 6099c5a c27ca9f 872314e f38b2d4 d3b84bb 1e26031 3982972; manifests .refactor/family-revision8-<family>.json; id maps .refactor/idmap-revision8-<family>.json; --test-mode mapping). Records: .refactor/decompose-records-revision8.json (commit, body snapshot, manifest, id snapshot per cluster). Plan .refactor/plan-revision8.md, critique .refactor/critique-revision8.md, debt .refactor/debt-revision8.md.

Disclosed non-move commits (all expected, none is scope creep):
1. 42980e7 HAND-WRITTEN constants module tests/_revision8_constants.py (brief section 4 item 2 and section 11 item 1): ROOT, TOOLS, RECORDED_AT statements moved verbatim plus the sys.path bootstrap copy; rope_move.py cannot move Path(__file__)-derived constants. The one hand-written commit of the session.
2. c8cf01a temporary ruff globs (config-only); cc99cc8 operator ruling: pyproject [tool.ruff.lint.isort] known-local-folder = [codex_orchestrator, codex_orch_tools, forge_cli, commitment_paths] (zero new findings repo-wide) so generated headers import tests._revision8_constants before codex_orchestrator; 5be2e31 provisional size-baseline entries; 97d4d60 finalize (measured entries, baseline, changelog, debt).
3. 4977efc operator ruling 14:10: tests/test_migration.py LegacyRuntimeNameCarveOutTests.allowed_prefixes gains '.refactor/' (one line), because the tracked tests/ body and ID snapshots carry that test's own name; the ONE permitted edit to a test outside the target (evidence-shipping question is bead forge-plugin-xlt7).
4. Every mover call used --import-root "$PWD" (section 11 item 2) so imports read from tests._revision8_...; the type step (REFACTOR_TYPE_CMD) runs mypy over scripts/forge/forge_cli, not tests/ (section 11 item 3).

Expected shape: source tests/test_revision8_coordination.py is a 10-line docstring-only shell (kept by plan section 0.3; collects 0 tests; deletion is follow-up forge-plugin-7pzp). No production file changed: git diff --stat 69bc28d..97d4d60 -- scripts docs/specs .forge is empty. No mint. Gate 1 (4 shards, 1908 tests) PASS at wave close (4977efc) and twice on 97d4d60 (logs .refactor/gate1-revision8-*.txt).
