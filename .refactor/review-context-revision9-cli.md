# Reviewer context for the revision9 cli-surfaces test split (branch refactor/split-test-revision9-cli-surfaces, BASE 69bc28d, reviewed tip = finalize commit ea0698b)

Session: bead forge-plugin-kzu0, lane B2 of docs/analysis/refactor-overnight-brief-2026-09-23.md, plugin refactor-python 0.1.3. Test-only decompose of the one class Revision9BoundCLIIntegrationTests (base CLI_FIXTURE_SUPPORT.ForgeCLIFixture, a runtime-loaded attribute): mixin shape for the 15 helpers (tests/_revision9_cli_support.py, commit ce6eb08, manifest .refactor/mixin-revision9-cli-mixin.json, --test-mode identity) and class shape for the 62 tests in five families (commits c424d79 dispositions, e31e727 start_locks, e9a0250 replay, 7596909 multicell_stack, e03515d ingest; manifests .refactor/family-revision9-cli-<family>.json; id maps .refactor/idmap-revision9-cli-<family>.json; --test-mode mapping). The eight other classes in the source were never in scope and stay. Records: .refactor/decompose-records-revision9-cli.json. Plan .refactor/plan-revision9-cli.md (revision 2), critique .refactor/critique-revision9-cli.md, debt .refactor/debt-revision9-cli.md. Lane B1 (branch refactor/split-test-revision8-coordination, handover forge-plugin-4p7p) is the sibling split reviewed earlier today; its consensus record .refactor/consensus-revision8.md on that branch settled the same class of findings (direct execution of a test module is not a supported surface; relocated test-module names have no importer; duplicated sys.path entries are pre-existing in kind).

Disclosed non-move commits (all expected, none is scope creep):
1. b4d3c4f HAND-WRITTEN constants module tests/_revision9_cli_constants.py (brief section 4 item 2 and section 11 item 1): the source's preamble lines 24..55 verbatim and in order (ROOT, CLI_PATH, ENVELOPE_KEYS, the tests._cli_loader import, CLI, CORE, RUNTIME, ENGINE, CANDIDATE, CLI_FIXTURE_SUPPORT, def key) plus the two imports they need; the only textual difference is the loader import wrapped by ruff --select I --fix. The source lost those lines and gained two import lines (the loader import kept because patch_engine is still read by the classes that stay; one import of the eight names still read). load_script/package_module cache in sys.modules so each loaded module is still created once. The one hand-written commit of the session.
2. a30d535 config-only prep: temporary ruff globs; the operator ruling of 11:30 (pyproject [tool.ruff.lint.isort] known-local-folder = [codex_orchestrator, codex_orch_tools, forge_cli, commitment_paths]); the operator ruling of 14:10 (tests/test_migration.py LegacyRuntimeNameCarveOutTests.allowed_prefixes += ".refactor/", the ONE permitted edit to a test outside the target; evidence-shipping question is bead forge-plugin-xlt7). Both rulings are byte-identical hunks to lane B1's cc99cc8 and 4977efc so either branch reintegrates first. 451cf2f provisional size-baseline entries; ea0698b finalize (measured entries, baseline, changelog, debt).
3. Every mover call used --import-root "$PWD" (section 11 item 2); the mover's repair_source_imports prunes the source imports each family leaves unused (dispositions: datetime, stat; start_locks: ROOT from the constants import; ingest: os, warnings, patch_chain_core from the loader import) in the same operation, manifest-covered; the type step (REFACTOR_TYPE_CMD) runs mypy over scripts/forge/forge_cli, not tests/ (section 11 item 3). The per-cluster gate also ran python3 -m unittest tests.test_cli_loader (brief section 5).

Expected shape: source tests/test_revision9_cli_surfaces.py = 1,229 code lines (eight out-of-scope classes + `class Revision9BoundCLIIntegrationTests(Revision9CliSupport, CLI_FIXTURE_SUPPORT.ForgeCLIFixture): pass` + __main__), over the 1,000 ceiling by design of the session's scope, pinned at 1229 with follow-up bead forge-plugin-by61. No production file changed: git diff --stat 69bc28d..ea0698b -- scripts docs/specs .forge is empty. No mint. Gate 1 (4 shards, 1908 tests) PASS at wave close and twice on ea0698b (logs .refactor/gate1-revision9-cli-*.txt).
