# Split plan: scripts/forge/forge_cli/app.py -> scripts/forge/forge_cli/app/

Inputs: `.refactor/inventory-app.md/.json` (18 symbols, 22 edges, 4 "hubs" auto-peeled), the AST list of every module-level statement in `app.py`, an AST patch census over `tests/` (E0), a grep census for `__file__`/`SCRIPT_DIR`/`sys.argv[0]`/`importlib`/`inspect` (E1), `.refactor/plan-engine.md` + `.refactor/critique-engine.md` (precedent and what the critic blocked), `docs/analysis/refactoring-review-2026-09-11.md` R4 slice 1 (follow-up seams), spec line 117, `policy.py` `REVIEWER_EVAL_TRIGGER_TABLE`, `pyproject.toml` (per-file-ignores line 60, contracts 223-296), `.refactor-baseline.json`, the FR-230 manifest. `radon`/`vulture` are not installed. Operator rulings on bead forge-plugin-hcuo are taken as given: pure rope moves, `MergeEngine` stays one class in one over-budget module, no mixin split here.

Method: the inventory graph above `MergeEngine` is tiny and decides everything. Two of its 22 edges are false positives — `prepare_merge_admission -> main` and `_observe_current_merge_candidate -> main` come from a LOCAL `main = chain_core.Repository(...)` (lines 596, 831) — so the real graph is: a self-contained deferred-mutation-journal cluster, two single-function leaves, `MergeEngine` (reads exactly `_persist_deferred_mutation_result`, `prepare_merge_admission`, `_observe_current_merge_candidate`), and the router/dispatch/`main` chain that constructs `MergeEngine`. No SCC, no quoted `"MergeEngine"` annotation (`grep -c '"MergeEngine"'` = 0 outside `__all__`), no `TYPE_CHECKING` guard needed. The four inventory "hubs" are not hubs (two are in-cluster, `MergeEngine` has one consumer cluster, `main` has none), so there is no wave-0 `_core.py`; wave 0 is the three independent leaves. 5 targets result plus the package root.

Conventions for the extractor (identical to the engine split): every target is `scripts/forge/forge_cli/app/<target>.py`; the source of every move is `scripts/forge/forge_cli/app/__init__.py` (after step E2), which keeps `__all__` verbatim (lines 11516-11524) plus one RELATIVE re-export `from .<owner> import <name> as <name>` per moved symbol (the form `engine/__init__.py` uses and the workflow's wave-close prep writes); intra-submodule imports are absolute (`from forge_cli.app._merge_engine import MergeEngine`); external imports are copied per target from the header (lines 9-43) as listed in each target section (the header binds `runtime` twice and `engine` twice — once as `_engine_module`; each target binds each once, except `_dispatch.py`, which needs BOTH `engine` and `_engine_module` because `dispatch`/`_merge_command_engine` shadow `engine` with a parameter and reach the module through the alias at lines 261 and 420); every module keeps `from __future__ import annotations`; `engine.<name>`, `chain_core.<name>`, `runtime.<name>` stay attribute reads (unchanged). No function or class body is edited (the `.refactor/before-app.json` oracle from the freeze commit 713a81c stands: there is no E1 body edit, see Pre-wave). Extractors never edit `.refactor-baseline.json`, `pyproject.toml`, the FR-230 manifest, `forge-project.md`, `policy.py`, or the spec.

## Delete first (dead code, with evidence)

- Nothing is deleted. `vulture` is not installed; by inventory every symbol has fan-in >= 1 except `__all__` (the shim contract) and `main` (called by `scripts/forge/cli.py:131`). `_MUTATION_JOURNAL_SIDEBAND_PREFIX` is read by `tests/test_mutation_runner.py:1542` through the package root (`app._MUTATION_JOURNAL_SIDEBAND_PREFIX`), so it must be re-exported.
- The header's duplicate imports (`runtime` at 20 and 27, `engine` at 26/28 — F811 grandfathered by the app.py per-file-ignores) are not "deleted": each target simply binds each name once; the root keeps only what its re-exports need (Risk 6).

## Target modules

Code lines are non-blank, non-comment lines of the moved symbols (docstrings included), measured with the `check_file_length.py` rule over the AST spans; header estimate in parentheses. Only `_merge_engine.py` is flagged OVER BUDGET.

### _mutation_journal.py — Deferred scoped-mutation journal sideband: the five `_MUTATION_*` constants, the private request error, output bounding, request validation and result persistence.
- symbols (dependency order): _MUTATION_JOURNAL_SCHEMA, _MUTATION_JOURNAL_SIDEBAND_PREFIX, _MUTATION_PERSISTENCE_ADVISORY, _MUTATION_JOURNAL_REQUEST_KEYS, _MUTATION_JOURNAL_PREIMAGE_KEYS, _DeferredMutationRequestError, _mutation_persistence_error, _bounded_mutation_output, _validate_deferred_mutation_request, _persist_deferred_mutation_result
- owns state: _MUTATION_JOURNAL_REQUEST_KEYS (a `frozenset` literal, lines 51-67; immutable, flagged by the inventory only because it is a set display), _MUTATION_JOURNAL_PREIMAGE_KEYS (tuple), the three string/bytes constants
- estimated code lines: 176 (+ ~12 header = ~190)
- imports from package: none (leaf); external: `Any`, `Sequence` (typing), `Path`, `dataclasses`, `json`, `chain_core`, `runtime`, `sha256_bytes`
- wave: 0
- cycle risks and resolution: none: leaf. Lines 46-234 are one contiguous block (constants 46-80, class 83-84, defs 87-234) with no comment lines between them (`grep -c '^#' app.py` = 0). `_persist_deferred_mutation_result` is a test patch target (E0 site 4).
- fallback seam (orchestrator-only, not expected): constants + error vs the four helpers

### _admission.py — Merge-start admission: `prepare_merge_admission` (source worktree/registered-worktree/common-dir/destination-ref/policy checks producing `engine.MergeAdmission`).
- symbols (dependency order): prepare_merge_admission
- owns state: none
- estimated code lines: 195 (+ ~12 header = ~207)
- imports from package: none (leaf); external: `Path`, `os`, `chain_core`, `engine`, `PolicyError`, `V2ReasonCode`, `parse_policy`, `sha256_bytes`
- wave: 0
- cycle risks and resolution: none: leaf (the inventory edge to `main` is the local variable at line 596). Read by `MergeEngine` at lines 2082, 3064, 3155, 3288.
- fallback seam: none (one function)

### _candidate_observation.py — Current-merge-candidate observation for the shared chain verbs: `_observe_current_merge_candidate` (repository, policy, changed paths of the candidate under review/verify).
- symbols (dependency order): _observe_current_merge_candidate
- owns state: none
- estimated code lines: 193 (+ ~14 header = ~207)
- imports from package: none (leaf); external: `Any`, `Mapping`, `Path`, `chain_core`, `engine`, `FrozenError`, `REVISION9_OUTPUT_SCHEMA`, `V2ReasonCode`, `Policy`, `PolicyError`, `parse_policy`, `sha256_bytes`
- wave: 0
- cycle risks and resolution: none: leaf (the `main` edge is the local at line 831). Read by `MergeEngine` at 14 sites (4399 … 11437) and patched by tests at 2 sites (E0 sites 2-3).
- fallback seam: none (one function)

### _merge_engine.py — The `MergeEngine` class (93 methods; 13 static/class-style methods never touch `self`; 57 nested defs; 7 methods over 300 lines). OVER BUDGET by operator ruling (forge-plugin-hcuo), exactly as `engine/_engine.py`.
- symbols (dependency order): MergeEngine
- owns state: none (class body is 93 `def`s plus its docstring; no class-level assignments, no decorators applied at class-body time from this module — only `staticmethod`/`classmethod`/`property`/`contextlib.contextmanager`)
- estimated code lines: 10,282 (+ ~35 header = ~10,320; OVER BUDGET: one unsplittable class; the provisional `.refactor-baseline.json` entry `"scripts/forge/forge_cli/app/_merge_engine.py": 10400` is committed by the orchestrator at the wave-0 close, BEFORE the wave-1 extractor runs, because the extractor gate (`verify.sh:36` -> `check_file_length.py` against the baseline) would otherwise fail deterministically and extractors may not touch the baseline; the finalize wave re-pins it to the measured count, Risk 3)
- imports from package: _mutation_journal (`_persist_deferred_mutation_result`), _admission (`prepare_merge_admission`), _candidate_observation (`_observe_current_merge_candidate`); external: `Any`, `Callable`, `Iterable`, `Mapping`, `Sequence`, `Path`, `base64`, `contextlib`, `copy`, `dataclasses`, `os`, `re`, `secrets`, `socket`, `stat`, `sys`, `chain_core`, `engine`, `runtime`, `FrozenError`, `Outcome`, `REVISION9_OUTPUT_SCHEMA`, `ReasonCode`, `Refusal`, `V2ReasonCode`, `Policy`, `sha256_bytes`
- wave: 1
- cycle risks and resolution: none; `MergeEngine` never names `dispatch`, `main`, `_route_shared_chain_engine` or `_merge_command_engine` (AST-verified), so `_dispatch -> _merge_engine` is the only edge into it and it points forward. Uses `nonlocal` in 22 nested closures (1924 … 6760) — closure-local, not module state; no `global` statement exists in `app.py`.
- fallback seam: none (no body edits; the class split is the Follow-up bead)

### _dispatch.py — Entry points: the shared chain-verb router, the merge-command engine factory, `dispatch`, and `main`.
- symbols (dependency order): _route_shared_chain_engine, _merge_command_engine, dispatch, main
- owns state: none
- estimated code lines: 289 (+ ~20 header = ~310)
- imports from package: _merge_engine (`MergeEngine`, constructed at lines 255 and 263 and named in the return annotations at 237/258); external: `Sequence`, `argparse`, `dataclasses`, `sys`, `chain_core`, `engine`, `engine as _engine_module` (lines 261, 420), `runtime`, `FrozenError`, `OUTPUT_SCHEMA`, `Outcome`, `REVISION9_OUTPUT_SCHEMA`, `ReasonCode`, `Refusal`, `V2ReasonCode`
- wave: 2
- cycle risks and resolution: none. `main` calls `dispatch`, `dispatch` calls the two factories by bare name — all four live here, which is why the three E0 patch sites on `dispatch`/`_merge_command_engine` need `patch_app` (a root-only patch would not reach this module's bindings). `scripts/forge/cli.py:131` calls `app.main()` through the root re-export.
- fallback seam (orchestrator-only, if the header pushes it past 400): `_dispatch.py` (router + factory + `dispatch`) vs `_main.py` (`main`), `_main` one wave later

### __init__.py — stays: the module docstring (lines 1-5, rewritten only in the finalize wave to describe the package), `from __future__ import annotations`, one re-export per moved symbol (17 lines), and `__all__` verbatim (lines 11516-11524, 7 names). Reason `__all__` stays: `scripts/forge/cli.py:92-93` forwards exactly this list through `__getattr__`, and `tests/test_cli_loader.py:19-29` builds `MOVED_NAMES` from it. ~32 code lines after the finalize wave (measure there).

## State ownership table

| state | owner module | users |
|---|---|---|
| `_MUTATION_JOURNAL_SCHEMA` | `_mutation_journal.py` | `_validate_deferred_mutation_request` |
| `_MUTATION_JOURNAL_SIDEBAND_PREFIX` | `_mutation_journal.py` | `_persist_deferred_mutation_result`; root reads `tests/test_mutation_runner.py:1542`, `tests/test_cli_merge_adapters.py:1192` |
| `_MUTATION_PERSISTENCE_ADVISORY` | `_mutation_journal.py` | `_mutation_persistence_error`, `_bounded_mutation_output`; root read `tests/test_cli_merge_adapters.py:1220` |
| `_persist_deferred_mutation_result` (function, listed for its root reads) | `_mutation_journal.py` | `MergeEngine` line 6228; root reads `tests/test_cli_merge_adapters.py:1112,1122,1165,1211,1394` (+ patch site 1043) |
| `_MUTATION_JOURNAL_REQUEST_KEYS` (frozenset) | `_mutation_journal.py` | `_validate_deferred_mutation_request` |
| `_MUTATION_JOURNAL_PREIMAGE_KEYS` (tuple) | `_mutation_journal.py` | `_validate_deferred_mutation_request` |
| `__all__` | `__init__.py` (stays) | `scripts/forge/cli.py:92`, `tests/test_cli_loader.py:21` |

No `global` statement exists; the 22 `nonlocal` statements (21 `nonlocal state`, 1 `nonlocal state, progress`) are closures inside `MergeEngine` methods and move with the class. Every owner is in a wave strictly before every user's wave (or the same file).

## Waves

Wave membership = closure over the 20 real edges (22 minus the two local-`main` false positives): a target is in wave N when every target it imports is in a wave < N; no edge joins two members of the same wave. A wave starts only after the previous wave is merged. The orchestrator runs the pre-wave steps E0, E2 and the finalize wave (wave 3) by hand; `.refactor/plan-app.json` `waves` covers only the rope extraction clusters (waves 0-2, one JSON entry per plan wave, same numbering; `notes` carry the verbatim-cut statements — there are none — and the import facts above). Each extractor: move the listed symbols from `app/__init__.py` (rope `move` or verbatim cut of the contiguous line span) into the target, add the imports the target needs, add `from .<target> import <name> as <name>` to `__init__.py` for every moved name, then run the gate: `ruff check scripts tests system/fr223`, `git ls-files -z "*.py" | xargs -0 python3 scripts/check_file_length.py`, `PYTHONPATH=scripts:scripts/forge lint-imports`, `snapshot_bodies.py` check against `.refactor/before-app.json` (bodies byte-identical), `type_baseline.py check` (keys are path-free normalized messages, so moves add no key), `python3 -c 'import forge_cli.app'`, and `tests.test_cli_loader` (the extended `patch_app` sweep). After each wave the ORCHESTRATOR (as in engine commits 60a2247/7fd7749/63b140e) adds the new submodule to the FR-230 manifest's `subjects.production` and re-mints (`python3 .codex-orchestrator/runs/run-20260906-95e4-phase3/evidence/mint.py`), because `tests/test_fr230_phase3_manifest.py:243` hashes every subject from disk and `app/__init__.py` shrinks every wave; then `tests.test_fr230_phase3_manifest` and `tests.test_fr223_v2_byte_pins` (the engine wave commits also touched a 2-line pin in `tests/test_fr223_v2_byte_pins.py` — check it).

Size rule: if any non-flagged target exceeds 500 code lines with its header after extraction, the orchestrator (never an extractor) splits it on the seam named in that target's section. Flagged (OVER BUDGET): `_merge_engine.py` only.

### Pre-wave (sequential, one worktree, two commits, all BEFORE wave 0)
- step E0 (tests only; mirrors the engine E0 commit `test(engine): patch engine controls through one loader helper`): in `tests/_cli_loader.py` add `_app_modules()` (= `_package_modules("app")`) and `patch_app(name, /, *args, **kwargs) -> _PackagePatch("app", ...)`; `_PackagePatch` already generalizes over the package name (it refuses names the root does not bind and restores in reverse). Rewrite the **6 sites on 4 names** found by AST census (all are multi-line `mock.patch.object(\n APP, "<name>", ...)` calls — a line grep for `patch.object(APP` finds 0, the sweep must stay AST-based); every one needs `patch_app` because the reader of each patched name moves OUT of `__init__.py`. Status: E0 is already applied in the working tree at 713a81c (uncommitted; `tests.test_cli_loader` 14 OK, ruff clean) and is committed as its own commit BEFORE E2 with the message `test(app): patch app controls through one loader helper`:
  1. `tests/test_cli_merge_lifecycle.py:5294` `_merge_command_engine` — read by `dispatch` (`_dispatch.py`).
  2. `tests/test_cli_merge_adapters.py:997` `_observe_current_merge_candidate` — read by `MergeEngine` (`_merge_engine.py`).
  3. `tests/test_cli_merge_integration.py:7726` `_observe_current_merge_candidate` — same.
  4. `tests/test_cli_merge_adapters.py:1043` `_persist_deferred_mutation_result` — read by `MergeEngine` line 6228.
  5. `tests/test_cli_merge_lifecycle.py:5157` `dispatch` — read by `main` (`_dispatch.py`).
  6. `tests/test_cli_merge_lifecycle.py:5178` `dispatch` — same.
  Stays (no change): the 5 `patch.object(CLI.MergeEngine, "<method>")` class-attribute patches (`test_cli_merge_integration.py:5248,5311,5400,5423,6567` — same class object wherever bound; already in the sweep's accepted list); `tests/test_cli_loader.py:365` `mock.patch.object(module, name, sentinel)` with `(app, "dispatch")` — asserts shim forwarding through the ROOT, which the root binding still satisfies; the 35 `patch.object(CLI.os|sys|subprocess|selectors, ...)` stdlib patches and the `CLI.Repository|MergeChainStore|Engine` class patches. No `mock.patch("forge_cli.app.<name>")`, no `APP.<name> = ...`/`CLI.<name> = ...` assignment and no `setattr(APP|CLI, ...)` exists in `tests/` (AST census over every `tests/*.py`). Extend `tests/test_cli_loader.py`: add `_is_app_module_alias(target)` (tail `APP`, or tail `app` with a CLI-shaped prefix such as `CLI.app`/`forge_cli.app`; never bare `app`, which the loader test uses as a local) and a third branch in `_moved_shim_patch_offenders` reporting `patches app name <name> on <target> outside patch_app`; extend `test_moved_name_patch_sweep_covers_cli_alias_forms` and add `test_app_patch_sweep_rejects_raw_module_patches_and_accepts_the_helper` (rejected: `mock.patch.object(APP, "dispatch", value)`, `patch.object(CLI.app, 'dispatch', value)`, `mock.patch("forge_cli.app.dispatch", value)`; accepted: `patch_app("dispatch", value)`, `mock.patch.object(CLI.MergeEngine, "finalize", value)`, `mock.patch.object(merge_engine, "_halt", value)`), plus `test_patch_app_reaches_every_app_module_and_restores` mirroring the engine one. Commit `test(app): patch app controls through one loader helper`.
- step E1 — **no finding, no commit.** `grep -n "__file__\|SCRIPT_DIR\|sys.argv\[0\]\|Path(__file__)\|importlib\|inspect\.\|__spec__\|__package__\|__getattr__\|TYPE_CHECKING\|if __name__" app.py` returns only line 530 (`type(exc).__name__`, a class name for a diagnostic). Nothing in `app.py` resolves a path from its own location, re-execs itself, loads a module by path, or inspects frames; the merge bootstrap child re-exec lives in `engine/_merge_bootstrap_child.py` (already E1-fixed). The body oracle `.refactor/before-app.json` (freeze commit 713a81c, 881 bodies) is therefore the final oracle; no operator decision is needed on this point.
- step E2 (package conversion, own commit; engine precedent d1ca21e): `git mv scripts/forge/forge_cli/app.py scripts/forge/forge_cli/app/__init__.py`; re-key `.refactor-baseline.json` line 9 from `scripts/forge/forge_cli/app.py` to `scripts/forge/forge_cli/app/__init__.py` with the SAME number (11183; may shrink, never grow); add the `pyproject.toml` per-file-ignores entry `"scripts/forge/forge_cli/app/*.py"` with the line-60 rule list (narrow per module in the finalize wave) and keep the line-60 entry until the finalize wave; in the FR-230 manifest replace the line-51 subject `scripts/forge/forge_cli/app.py` with `scripts/forge/forge_cli/app/__init__.py` and re-mint (the five `tests/fixtures/fr230-results/fr230-phase-3-*.json` result digests change, as in d1ca21e) (`.refactor/rope-ignore.txt` already names `forge_cli.app` since 713a81c). Nothing else changes. Gate; commit `refactor(app): convert module to package (no code moved)`. Reviewer: `git ls-files scripts/forge/forge_cli/app.py` is empty from E2 on; `tests.test_repo_conformance` passes (no file under `forge_cli/` is executable).

### Wave 0 (sequential: three leaves; every cluster rewrites `app/__init__.py`, so extraction is sequential from merged HEAD, one commit per cluster; no edge between them)
- cluster "mutation journal": target=_mutation_journal.py symbols=[_MUTATION_JOURNAL_SCHEMA, _MUTATION_JOURNAL_SIDEBAND_PREFIX, _MUTATION_PERSISTENCE_ADVISORY, _MUTATION_JOURNAL_REQUEST_KEYS, _MUTATION_JOURNAL_PREIMAGE_KEYS, _DeferredMutationRequestError, _mutation_persistence_error, _bounded_mutation_output, _validate_deferred_mutation_request, _persist_deferred_mutation_result] (contiguous lines 46-234)
- cluster "admission": target=_admission.py symbols=[prepare_merge_admission] (lines 543-740)
- cluster "candidate observation": target=_candidate_observation.py symbols=[_observe_current_merge_candidate] (lines 743-937)

### Wave-0 close (orchestrator, by hand, own commit, after the wave-0 re-mint and BEFORE wave 1)
- Add `"scripts/forge/forge_cli/app/_merge_engine.py": 10400` to `.refactor-baseline.json` under the forge-plugin-hcuo ruling (engine precedent: e8f6e30 provisional `_engine.py` 3700, then 7263e40 extract, then e23ac60 re-pin). Commit `refactor(app): provisional size-baseline entry for the planned over-budget app/_merge_engine.py`. `check_file_length.py` accepts a baseline key for a file that does not exist yet (as it did for e8f6e30).

### Wave 1 (single cluster, after wave 0 merged and the wave-0 close commit landed; imports all three wave-0 modules)
- cluster "merge engine": target=_merge_engine.py symbols=[MergeEngine] (lines 940-11513; OVER BUDGET — the extractor does not touch `.refactor-baseline.json`; its gate passes because the provisional 10400 entry already exists; the finalize wave re-pins the entry to the measured count, Risk 3)

### Wave 2 (single cluster, after wave 1 merged; imports `_merge_engine`)
- cluster "dispatch": target=_dispatch.py symbols=[_route_shared_chain_engine, _merge_command_engine, dispatch, main] (contiguous lines 237-540; binds BOTH `engine` and `_engine_module`)

### Wave 3 (finalize; orchestrator, by hand, control-class, binding review + explicit operator approval)
- `app/__init__.py`: docstring rewritten to describe the package (mirror the engine root docstring, b764d5a); verify it is docstring + `__future__` + 17 re-exports + `__all__` only; no unused header import re-bound (Risk 6).
- `.refactor-baseline.json`: delete the `app/__init__.py` entry (final count ~32 <= 500); re-pin the provisional `scripts/forge/forge_cli/app/_merge_engine.py` entry (10400) to `check_file_length.py`'s measured count (~10,320; engine precedent e23ac60) — the ruling on forge-plugin-hcuo is the operator direction the entry cites.
- `pyproject.toml`: delete the line-60 `scripts/forge/forge_cli/app.py` per-file-ignores entry; replace the `app/*.py` entry with one narrowed entry per module from `ruff check --select ALL`-style output as chain_core/engine did (`_merge_engine.py` keeps the full list minus what ruff no longer reports); add the contract below `forge_cli.engine layers` (lines 284-296): `[[tool.importlinter.contracts]] name = "forge_cli.app layers" type = "layers" containers = ["forge_cli.app"] layers = ["_dispatch", "_merge_engine", "_admission | _candidate_observation | _mutation_journal"]` with the same three `forge_cli.runtime -> codex_orchestrator.*` `ignore_imports` (the indirect lazy cycle reaches every submodule, as the engine contract's comment records). No `TYPE_CHECKING` edge exists in this package, so nothing relies on the session-level `exclude_type_checking_imports`. The existing `forge_cli layers` contract (line 223-229) lists `forge_cli.app` at the top: a package satisfies a layer exactly as `forge_cli.engine`/`forge_cli.chain_core` already do — no change.
- `docs/specs/forge-plugin-spec.md` line 117, the clause to rewrite: "and `app.py` (`MergeEngine`, the shared chain-verb router, argument-parsing and dispatch entry points, and `main`; it obtains the parser and other engine helpers through `forge_cli.engine`)" -> describe the `app/` package with the engine wording pattern: root `__init__.py` holds the verbatim `__all__` and re-exports every symbol the historical `app.py` defined; the root and the submodules are exactly the `scripts/forge/forge_cli/app/` production subjects listed in the FR-230 manifest; submodules hold the `MergeEngine` class (`_merge_engine.py`), merge-start admission, current-candidate observation, the deferred mutation-journal sideband, and the router/dispatch/`main` entry points; it obtains the parser and other engine helpers through `forge_cli.engine`. No test pins that sentence (grep of `tests/test_docs_contract.py`, `test_repo_conformance.py`, `test_fr230_phase3_manifest.py` for `app.py` finds nothing). `docs/specs/**` -> STRICT evals + binding review + explicit operator approval.
- FR-230 manifest: after wave 2 the `subjects.production` list carries the six `app/` files (`__init__.py`, `_admission.py`, `_candidate_observation.py`, `_dispatch.py`, `_merge_engine.py`, `_mutation_journal.py`) and no `app.py`; final re-mint; `tests.test_fr230_phase3_manifest` + `tests.test_fr223_v2_byte_pins`.
- Reviewer-facing eval trigger table: **no row names `app.py`** — verified by grep of `forge-project.md`, `system/template/forge-project.md`, `AGENTS.md`, `scripts/forge/forge_cli/policy.py` (`REVIEWER_EVAL_TRIGGER_TABLE`) and `docs/specs/forge-plugin-spec.md` (only line 117 mentions `app.py`; the trigger rows at 278-285 name `engine/**` only). No Risk-7-style window, no operator decision.
- CHANGELOG `[Unreleased]` entry; `tests.test_repo_conformance`; full gate-1 twice.

## Re-exports required in __init__.py

`__all__` (7 names, verbatim, order unchanged): `MergeEngine`, `_merge_command_engine`, `_observe_current_merge_candidate`, `_route_shared_chain_engine`, `dispatch`, `main`, `prepare_merge_admission` — `scripts/forge/cli.py:92-93` forwards exactly this list and `cli.py:131` calls `app.main()`.

Rule: `__init__.py` re-exports ALL 17 moved top-level symbols (7 in `__all__` + 10 not) as `from .<owner> import <name> as <name>` (relative, on the root only): the 10 outside `__all__` are `_MUTATION_JOURNAL_SCHEMA`, `_MUTATION_JOURNAL_SIDEBAND_PREFIX` (read by `tests/test_mutation_runner.py:1542` via the root), `_MUTATION_PERSISTENCE_ADVISORY`, `_MUTATION_JOURNAL_REQUEST_KEYS`, `_MUTATION_JOURNAL_PREIMAGE_KEYS`, `_DeferredMutationRequestError`, `_mutation_persistence_error`, `_bounded_mutation_output`, `_validate_deferred_mutation_request`, `_persist_deferred_mutation_result` (patched via the root at E0 site 4 and read via the root at `test_cli_merge_adapters.py:1112,1122,1165,1211,1394`; `patch_app` refuses names the root does not bind); `_MUTATION_PERSISTENCE_ADVISORY` is read via the root at `test_cli_merge_adapters.py:1220`, `_MUTATION_JOURNAL_SIDEBAND_PREFIX` at `:1192`. `__all__` is not extended. Every symbol in the inventory is placed: 17 moved (above), `__all__` stays.

## Risks the reviewer must check

1. **Patch targets (E0, 6 sites, 4 names).** After wave 1 the readers of `_observe_current_merge_candidate` and `_persist_deferred_mutation_result` bind them in `_merge_engine.py`; after wave 2 `dispatch` and `_merge_command_engine` are bound in `_dispatch.py`. A root-only patch silently stops intercepting, and tests 5-6 (`dispatch must not run`) would pass vacuously while `main` ran the real `dispatch`. Reviewer: (a) the six sites use `patch_app`; (b) the extended sweep in `tests/test_cli_loader.py` rejects `patch.object(APP, ...)`, `CLI.app`, and `patch("forge_cli.app.…")`, and is AST-based (the sites are multi-line); (c) `patch_app` restores on exit and yields the root replacement value; (d) `CLI.MergeEngine` class-attribute patches are untouched and still accepted by the sweep.
2. **No E1 body edit — verify the negative, not the fix.** `app.py` has no `__file__`, `SCRIPT_DIR`, `sys.argv[0]`, `importlib`, `inspect`, `__spec__`, `__getattr__`, `TYPE_CHECKING` or `if __name__` use (line 530 is `type(exc).__name__`). Reviewer: rerun that grep on `app/__init__.py` at E2 and confirm `snapshot_bodies.py` reports byte-identical bodies against `.refactor/before-app.json` (taken at 713a81c) after every wave — there is no post-E1 re-snapshot in this split, so any body drift is a defect.
3. **Budget / baseline (orchestrator-only).** `_merge_engine.py` (~10,320 code lines) is over budget by operator ruling; its provisional baseline entry (10400) MUST be committed at the wave-0 close, before the wave-1 extractor's `verify.sh` gate runs `check_file_length.py` (verify.sh:36) — otherwise wave 1 fails deterministically and the retry fails the same way (engine precedent e8f6e30 -> 7263e40 -> e23ac60). `app/__init__.py` is over budget from E2 until wave 1 lands (re-keyed entry, shrinking every wave), then ~320 after wave 1 and ~32 after the finalize wave. Extractors never edit `.refactor-baseline.json`; the reviewer confirms no body was edited to fit, that the wave-0-close commit precedes the wave-1 extraction in `git log`, and that the finalize wave re-pins `_merge_engine.py` to the measured count (10,3xx < 10400).
4. **Import-time side effects: none.** Every module-level statement in `app.py` is a docstring (1-5), an import (7-43), a constant (46-80), a `class`/`def`, or `__all__` (11516-11524): no `runtime.<x> = ...` seam binding, no marker loop, no lock/regex construction, no `__getattr__`, no `if __name__`. Reviewer: the set of `ast.parse(...).body` node types of `app/__init__.py` is {Expr, Import, ImportFrom, Assign, ClassDef, FunctionDef} at E2 and {Expr, ImportFrom, Assign} after the finalize wave.
5. **Attribute parity and the shim.** After the finalize wave `set(dir(forge_cli.app))` contains all 18 original top-level names; `scripts/forge/cli.py` needs no change; spec line 117 says the split changes no verb, diagnostic, reason code, `--help` byte, or corpus: run the FR-223 corpora (`tests.test_fr223_v2_byte_pins`), `tests.test_cli_loader.test_engine_and_app_are_canonical_and_forwarded_by_the_shim`, and `tests.test_repo_conformance`.
6. **Header imports and F401/F811.** The root header binds `runtime` and `engine` twice; each target binds once, `_dispatch.py` binds `engine` AND `engine as _engine_module` (lines 261/420 read the alias because `engine` is a parameter there). Reviewer: `ruff check scripts/forge/forge_cli/app/` passes with the narrowed per-module ignores; no unused import is re-bound on the root.
7. **Package cycle / layers contract.** Only `scripts/forge/cli.py:76` imports `forge_cli.app`; nothing under `engine/`, `chain_core/`, `codex_orchestrator/` or `scripts/forge/*.py` does (grep at 713a81c). `app` reads `engine.*`/`chain_core.*`/`runtime.*` by attribute; the `forge_cli layers` contract (app above engine) holds unchanged, and the new `forge_cli.app layers` contract is acyclic by construction (`_dispatch -> _merge_engine -> {_admission, _candidate_observation, _mutation_journal}`). Reviewer: `PYTHONPATH=scripts:scripts/forge lint-imports` after the finalize wave.
8. **FR-230 manifest per wave.** `tests/test_fr230_phase3_manifest.py:243` hashes every `subjects.production` path from disk; each wave changes `app/__init__.py` bytes and adds a file. Reviewer: every wave's merge commit carries the subject-list change (new submodule added) and a re-mint, the five `tests/fixtures/fr230-results/*.json` digests move with it, and the finalize wave's list is exactly the six `app/` files.
9. **Wave discipline and trigger tables.** Waves 1 and 2 are single clusters and must not run beside anything (critique-engine B3); wave 0's three clusters may share worktrees but stay one commit each. No reviewer-facing eval trigger row names `app.py`: `grep -rn "forge_cli/app" forge-project.md system/template/forge-project.md AGENTS.md scripts/forge/forge_cli/policy.py docs/specs/forge-plugin-spec.md` returns only spec line 117 (its rewritten sentence after the finalize wave).

## Follow-up (separate beads, not this split)

- **Split `MergeEngine` (10,282 code lines, 93 methods) into a class family — a class-structure change with its own tests, binding review and operator approval, NOT a rope move.** Seams from `docs/analysis/refactoring-review-2026-09-11.md` R4 slice 1, i.e. `forge_cli/app/merge/{admission,lock,bootstrap,epoch,observation,recovery,cleanup}.py` (or mixins composed into `MergeEngine`), anchored on the method-size facts: `_recover_conflict_locked` 843 lines @9133 and `recover` 616 @10321 (+ `_recover_rebase_observation_locked` 207, `_recover_classifying_bootstrap_v12_locked` 216: the recovery family is ~2,060 lines), `_recording_common_lock` 559 @1243 (lock), `_run_remote_observation` 495 @6589 + `_run_candidate_observation_locked` 199 + `_run_conflict_observation_locked` 235 + `_run_integrated_rebase_observation_locked` 236 (observation), `cleanup_chain` 453 @10938 + `_run_cleanup_child` 167 (cleanup), `_run_bootstrap_generation_composite` 412 @2500 (bootstrap), `_run_epoch_suite` 382 @6126 + `_run_epoch_push` 213 + `_complete_epoch_fetch_locked` 213 (epoch), `start_chain` 160 @3047 + `prepare_merge_admission` (admission). The 13 methods that never reference `self` (`_final_mode_unavailable`, `_recover_can_reach_final_mode`, `_bootstrap_fetch_argv`, `_resolved_fetch_tip`, `_wrong_state`, `_sealed_plan`, `_epoch_fetch_argv`, `_resolved_epoch_fetch_tip`, `_parse_remote_observation`, `_parse_fetched_remote_observation`, `_head_contained`, `_push_classification`, `_current_merge_authority`) are module-function candidates first (`_head_contained` is patched on the class at `test_cli_merge_integration.py:6567` — retarget with the move); the 57 nested defs and 22 `nonlocal` closures mark the methods whose extraction needs a state object. Each slice carries its FR-230 subject addition and strict-evals binding (R4 table).
- Narrow the per-module ruff ignores for `app/*.py` further as modules are cleaned.
- `tests/test_cli_loader.py`: fold `patch_chain_core`/`patch_engine`/`patch_app` into one `patch_package(package, name, ...)` once a fourth package appears.
