# Fable review: forge_cli.app split (branch refactor/split-app)
Reviewer: refactor-python:refactor-reviewer (Fable 5.1) run by the split-app workflow wf_e219f9ff-718 on 2026-09-18; tree reviewed: HEAD afce2e6 (finalize efc7b20 + Codex advisories); baseline b764d5a; Codex review `.refactor/codex-review-app.md` (thread 01a0b571-c486-74a3-889a-7296be03ac6c).
## Verdict: REJECT
### Blocking
- scripts/forge/forge_cli/app/_dispatch.py:1-4 and scripts/forge/forge_cli/app/_merge_engine.py:15-18 — commit afce2e6 changed the bytes of two FR-230 production subjects (import-header rewrite) without re-minting .forge/evals/tasks/fr230-phase3-4-v2.manifest.json and the five tests/fixtures/fr230-results/fr230-phase-3-*.json fixtures (git show --stat afce2e6 touches neither). The subject_candidate_sha256 bound to each PASS result is stale: tests.test_fr230_phase3_manifest is 12/12 OK on a git-archive extraction of efc7b20 and fails 3 tests at HEAD ('PASS result evidence is unresolved: cleanup, merge-last-line, merge-transition, re-verification-matrix, recovery'). Plan Risk 8 and the spec's 'every changed or added production subject requires a current strict-evals result binding' require the re-mint; gate-1 full discovery fails. Fix: re-mint (mint.py), commit the manifest + five fixtures, and re-pin the manifest digest in tests/test_fr223_v2_byte_pins.py.

Resolution: commit cc2cf98 re-mints the FR-230 manifest, the five result fixtures and the byte pin after the import-header prune; `tests.test_fr230_phase3_manifest` and `tests.test_fr223_v2_byte_pins` pass at cc2cf98 (orchestrator run); full gate-1 pair re-run on cc2cf98 (see bead forge-plugin-hcuo).

### Disputed Codex findings (adjudicated)
- Codex B1 (scripts/forge/forge_cli/app/__init__.py:37 — Re-exports are snapshot bindings: rebinding public forge_cli.app.dispatch no longer changes what main() invokes at _dispatch.py:296. The new patch_app helper compensates only tests; ordinary callers changed behavior.) — REFUTED: mechanically true (probe: _dispatch.dispatch is app.dispatch -> False after a root rebind; patch_app reaches _dispatch -> True) but it is the documented, inherent property of every package split in this repo: the engine root at baseline b764d5a carries the identical 'Re-exports are snapshots ... tests patch through patch_engine' wording (engine/__init__.py:15-16), and a repo-wide grep of scripts/ hooks/ skills/ system/ agents/ rules/ finds no non-test rebinding or setattr on any app attribute. No ordinary caller exists whose behavior changed.
- Codex B2 (scripts/forge/forge_cli/app/__init__.py:18 — The root drops 35 previously importable bindings, including Policy, Outcome, Path, engine, runtime, and chain_core.) — REFUTED: plan Risk 6 and the 'Re-exports required' list (7 __all__ + 10 private names) define the API and are all importable; the chain_core and engine precedents apply the same rule (engine root also lacks Policy); scripts/forge/cli.py forwards only __all__; a repo-wide grep finds no reader of any dropped header name on APP / CLI.app / forge_cli.app (the single APP.os hit is inside a synthetic sample string in tests/test_cli_loader.py:306, not executed code); the AST census over tests/ finds zero attribute reads of names the root no longer binds.
- Codex A1 (scripts/forge/forge_cli/app/_mutation_journal.py:4 — Import-start order changed from chain_core -> runtime to runtime -> chain_core; runtime initializes a cache and lock. Fresh imports pass, but initialization order is not preserved.) — REFUTED: scripts/forge/cli.py:73-76 imports runtime, chain_core, engine before app, and chain_core's own submodules import runtime (chain_core/_activation.py:41 and others), so app's header order never determines first initialization of either module in any real entry path.

### allowedChanged

[] (no body change approved)

### Summary

Own check clean: body oracle over scripts/forge/forge_cli 881/881 (missing=0 added=0 changed=0); every moved line span byte-identical to baseline app.py (rope only added blank lines between the five _MUTATION_* constants); literal multiset differs only in docstrings; root __init__.py is docstring + __future__ + 17 relative re-exports + verbatim __all__; module-level statements are imports/defs/constants only (no side effects, no function-local imports, no submodule importing the package root); all 17 moved names importable from forge_cli.app and the cli.py shim forwards identically; the six E0 sites use patch_app, the sweep is AST-based and extended, and an AST census over tests/ finds zero raw patches/assignments/dropped-name reads on app aliases; ruff clean, 183 files within budget, 5 import-linter contracts kept, REFACTOR_TEST_CMD 211 OK, byte pins and repo conformance OK; commit ordering (provisional baseline d788d70 before wave-1 bfd1196) and the _merge_engine.py re-pin (measured 10302) verified. ONE BLOCKING defect: afce2e6 (post-Codex advisories fix) rewrote the import headers of _dispatch.py and _merge_engine.py — both FR-230 production subjects — without re-minting the manifest/fixtures, so tests.test_fr230_phase3_manifest fails 3 tests at HEAD (passes 12/12 at efc7b20). Mechanical fix: re-mint, commit manifest + five fixtures, re-pin the manifest sha in tests/test_fr223_v2_byte_pins.py. Advisories: .refactor-baseline.json tightens untouched tests/test_cli_chain.py 2640->2635 (stale entry, permitted shrink, out of split scope); .refactor/type-baseline.json still cites app.py:10047 as a stale 'where' (keys path-free, harmless); provisional baseline landed before wave 0 rather than at wave-0 close (harmless). Codex (REJECT, reviewed efc7b20): B1 REFUTED, B2 REFUTED, A1 REFUTED (all in disputed); A2 and A5 already fixed at HEAD by afce2e6; A3 and A4 CONFIRMED as advisory facts only; A6 unverifiable on Codex's side, supplied by my run. allowedChanged is empty: no body change is approved.

## Re-verification of the resolved tip (2026-09-18)

Reviewer: refactor-python:refactor-reviewer (Fable 5.1), read-only, at HEAD c0c4f3c. **Verdict: APPROVE**, blocking [], disputed [], allowedChanged [].

- cc2cf98 touches exactly the manifest, the five FR-230 result fixtures and the byte pin; c0c4f3c touches only the four `.refactor/*-app` records.
- `tests.test_fr230_phase3_manifest tests.test_fr223_v2_byte_pins tests.test_docs_contract tests.test_repo_conformance tests.test_cli_loader tests.test_migration`: 122 tests OK.
- Manifest production subjects are exactly the six `app/` files; the manifest sha256 equals the byte pin; `subject_candidate()` recomputed from HEAD bytes equals the `subject_candidate_sha256` in all five fixtures (verified independently of the tests).
- Strict oracle 881/881 unchanged; ruff, size budget (183 files) and import-linter (5 contracts, `forge_cli.app layers` kept) pass; tree clean; `.refactor/` differs from main only in the 13 expected split files.
