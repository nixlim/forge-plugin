# Fable review of the finalized engine split (refactor-python:refactor-reviewer)

Branch refactor/split-engine, HEAD bdaf57c, base 0e41964; Codex review at .refactor/codex-review-engine.md.

VERDICT: APPROVE
CODEX_VERDICT: REJECT (high confidence; written against a pre-bdaf57c tip — two of its six blocking items and three advisories are stale at HEAD)

OWN CHECK (done before reading the Codex file)
- Oracle: `snapshot_bodies.py compare .refactor/before-engine.json scripts/forge/forge_cli --strict` -> 881 before, 881 after, missing=0 added=0 changed=0. (Running compare against `scripts/forge/forge_cli/engine` alone, as the method text suggests, yields 642 spurious MISSING because the snapshot covers all 51 forge_cli files; the correct root is scripts/forge/forge_cli.) Independent oracle from the baseline commit itself (git archive 0e41964 -> snapshot -> compare HEAD): exactly one CHANGED, `_merge_bootstrap_child_argv` (the sanctioned E1 edit); nothing else in forge_cli changed. Module-level constants: AST-dump parity of all 33 baseline top-level assignments vs their new owners — identical (so every reason/refusal literal, REVIEW_INSTRUCTION, REVIEW_LAUNCHER_CODE, GLOBAL_OPTIONS_HELP, SECRET_RULES are byte-identical); `__all__` literal identical (191 names, order unchanged).
- E1 (scripts/forge/forge_cli/engine/_merge_bootstrap_child.py:244): the only expression change is `Path(__file__)` -> `Path(runtime.__file__)`; `runtime` is imported at module top; tests/test_engine_bootstrap_reexec.py covers both unpatched and patched SCRIPT_DIR. Explicitly approved -> ALLOWED_CHANGED.
- E0 (tests/_cli_loader.py): `_PackagePatch` generalises the chain_core helper; restoring context manager; refuses names the root does not bind. Raw `patch.object(ENGINE, ...)`/`patch("forge_cli.engine.` sites outside the helper: none in tests/ (the only textual hits are synthetic fixture strings inside the sweep tests at tests/test_cli_loader.py:200-251). One direct root assignment remains at tests/test_cli_chain.py:86, immediately followed by the binder loop (commit a7212a8).
- Seam-marker loop move (chain_core/__init__.py -> _commit_chain.py:1076): setattr -> attribute assignment is semantically identical; all four callables are bound before the loop; `_seam` re-exported on the root for parity; operator-directed (bead 4j7 comment 2026-09-16).
- Import graph: no submodule imports the package root; no function-local imports; the only `_engine` references outside __init__ are the two `if TYPE_CHECKING:` guards (_command_lock.py:9, _finalize.py:7) covering exactly the three quoted "Engine" annotations plus the FinalizeContext field; `dataclasses.fields(FinalizeContext)[0].type == "Engine"` (string, never evaluated). Import-time side effect: exactly one, `runtime._build_chain_journal_records = _build_chain_journal_records` at _journal.py:480; identity probe True. Duplicated state: none — `_ARCHIVE_MODULE`/`_ARCHIVE_MODULE_LOCK` live once in _archive.py with the `global` rebinder; `_CHAIN_CAPABILITY_LOCK`, `_CHAIN_CAPABILITIES`, `PLACEHOLDER_RE`, `_DERIVE_MERGE_SCOPE` once in _state.py; root re-exports are the same objects (identity checks True).
- Public API: all 191 `__all__` names and all 31 plan-listed non-`__all__` names resolve on `forge_cli.engine`; all 223 baseline-defined top-level names present; `cli.Engine is forge_cli.engine.Engine`.
- Gates: ruff clean; `lint-imports` 4 contracts kept (new `forge_cli.engine layers`); check_file_length: 178 files within budget; test_repo_conformance OK (22); REFACTOR_TEST_CMD: 219 tests OK; Option A step-7 trigger-table one-liner passes; zero `forge_cli/engine.py` literals remain in the spec, policy.py, the three rendered copies, the four verbatim fixtures, test_fresh_eval_policy.py, test_cli_chain.py and the FR-230 manifest.
- Scope: every non-move hunk is one of: E0 (tests), E1 (one expression + test), seam-loop move (operator-directed), trigger-table retarget + re-mints (spec 117/118/278/280/285, policy.py:71/75, rendered copies, fixtures, FR-230 manifest and result-fixture sha256s, forge-project template pin in test_commit_and_region_template.py, FR-223 v2 artifact pin), pyproject per-file-ignores narrowing + import contract, .refactor-baseline.json re-pin, CHANGELOG. Plus cosmetic line-collapsing in tests/test_cli_chain.py (see advisory).

BLOCKING:
- none

ADVISORY:
- scripts/forge/forge_cli/engine/__init__.py (re-export design, inherent to any by-name split): assigning a control on the package root no longer reaches submodule readers (reproduced: `engine.REVIEW_MASTER_WINDOW_BYTES = 1` -> `_review_master_window_count(10)` is 1 at HEAD vs 10 at baseline). No production writer exists (grep over scripts/, tests/, hooks/, skills/: the only root write is the tests/test_cli_chain.py:86 fixture, which then assigns every binder); tests go through `patch_engine`, the loader sweep rejects raw root patches, and the merged chain_core split (6502849) set the same precedent. Not a behaviour change on any exercised path.
- scripts/forge/forge_cli/engine/__init__.py `_ARCHIVE_MODULE` re-export is an import-time snapshot (None) and never reflects the `_archive_module()` cache (reproduced). Plan Risk 6 acknowledged it and asked that the `__init__` docstring document it; the docstring does not. No reader in the repo. Follow-up bead + docstring note.
- scripts/forge/forge_cli/engine/_finalize.py:7, _command_lock.py:9: `typing.get_type_hints(FinalizeContext | _peek_selected_chain | _command_run_lock_id)` now raises NameError('Engine') (reproduced). No caller of get_type_hints in scripts/ or tests/; dataclasses never evaluate the string. Latent introspection difference only; plan-sanctioned resolution of the sole SCC.
- 21 incidental import bindings the old module exposed are no longer attributes of forge_cli.engine: Callable, Collection, Iterable, OUTPUT_SCHEMA, Policy, REGION_ORDER, Revision9ReasonCode, argparse, base64, binascii, candidate_module, contextlib, dataclasses, dt, errno, fresh_eval_module, functools, importlib, io, selectors, threading. None is in `__all__` (so the cli shim never forwarded them), none is read via any engine alias or explicitly imported anywhere in scripts/, tests/, hooks/, skills/; spec line 117 now states this rule. Root `__annotations__` went from 6 entries to {} and `__module__` of moved symbols now names submodules — inherent; no pickle/introspection user in the repo.
- scripts/forge/forge_cli/chain_core/_commit_chain.py:1076: `importlib.reload(forge_cli.chain_core)` no longer re-marks the four seam callables (reproduced: markers False after delattr + reload). Nothing in the repo reloads chain_core (`importlib.reload` absent; tests/_cli_loader.py re-executes only the cli.py shim). Operator-directed move; the reintegrating session should have bead 4j7's 2026-09-16 decision on record.
- .refactor-baseline.json:13 raises the grandfathered `_commit_chain.py` entry 1840 -> 1847 (measured 1847). This contradicts the AGENTS.md "may shrink, never grow" rule on its face; it is covered by the same recorded operator decision (bead 4j7, CHANGELOG line 13). Confirm the direction is bound to the reintegration candidate at Gate 4.
- tests/test_cli_chain.py: several purely cosmetic one-line collapses (dict literals, POLICY.replace calls) ride in the E0 hunks; behaviour-neutral, but not a move/import/re-export hunk. Note for the record.
- scripts/forge/forge_cli/engine/__init__.py docstring still reads "Moved verbatim from scripts/forge/cli.py ..." and does not describe the package layout as plan wave 6 required (the spec sentence does).

CODEX_FINDINGS:
- CONFIRMED (mechanism) / downgraded to ADVISORY — detached root controls (`REVIEW_MASTER_WINDOW_BYTES`, `CODEX_EXECUTABLE`) — reproduced HEAD 1 vs base 10; no production writer; test layer covered by patch_engine + sweep + binder fixture.
- CONFIRMED / ADVISORY — `_ARCHIVE_MODULE` root snapshot — reproduced; plan Risk 6; no reader.
- CONFIRMED / ADVISORY — `get_type_hints` NameError on the three TYPE_CHECKING-guarded annotations — reproduced; no caller.
- CONFIRMED / ADVISORY — 21 dropped incidental import bindings — reproduced list; none in `__all__`, no reader, spec 117 states the rule.
- REFUTED — journal seam assignment at `__init__.py:69` — at HEAD it is at _journal.py:480 (fixed by bdaf57c). -> DISPUTED.
- CONFIRMED (reload mechanism) / REFUTED ("unplanned") — chain_core seam-marker move — reload probe reproduced; no reloader in repo; operator-directed per bead 4j7. -> mechanism ADVISORY, "unplanned" DISPUTED.
- CONFIRMED / ADVISORY — `.refactor-baseline.json` _commit_chain 1840 -> 1847 — diff confirmed; operator direction recorded.
- CONFIRMED / ADVISORY — `__module__` paths, pickle paths, root `__annotations__` {} — annotations probe 6 -> 0; inherent; no user.
- REFUTED — spec 117-118 wording — no `engine.py` literal remains; package sentence and seam location are correct at HEAD. -> DISPUTED.
- REFUTED — "plan says 62 sites" — plan says 64. -> DISPUTED.
- REFUTED — "fixture creates CODEX_EXECUTABLE on every submodule" — guarded by hasattr. -> DISPUTED.

ALLOWED_CHANGED: _merge_bootstrap_child_argv (the sanctioned E1 edit: one expression `Path(__file__)` -> `Path(runtime.__file__)`, verified against the baseline commit; the shipped post-E1 before-engine.json compare already reports changed=0)

ALLOWED_CHANGED: _merge_bootstrap_child_argv
