# UP037 probe (separate worktree of the final sim tree a0e86fc): can the quoted self annotations be auto-fixed inside a tier-1 cluster?
# (first attempt without --isolated was INVALID: the worktree's own glob ignores UP037, so ruff fixed nothing; this run uses --isolated)

## 1. ruff --isolated --target-version py310 --select UP037 --fix on the last cluster's destination
Found 3 errors (3 fixed, 0 remaining).
[exit 0]
 scripts/forge/forge_cli/app/_engine_finalize.py | 6 +++---
 1 file changed, 3 insertions(+), 3 deletions(-)
-    self: "MergeEngine", state: dict[str, Any], lease: chain_core.ChainLease
+    self: MergeEngine, state: dict[str, Any], lease: chain_core.ChainLease
-    self: "MergeEngine",
+    self: MergeEngine,

## 2. manifest oracle for c29 against its own before-snapshot, AFTER that fix (tree differs from mover output)
decompose oracle: FAIL: undeclared structural change: scripts/forge/forge_cli/app/_engine_finalize.py
[oracle exit 1]

## 3. control: same oracle on the unfixed tree
manifest oracle: PASS (tier 1)
[oracle exit 0]
