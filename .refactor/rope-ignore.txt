# Resources rope_move.py must not see while splitting forge_cli.app (formerly engine, chain_core; read by the
# plugin's rope_move.py, one glob per line). Importers outside the package stay untouched:
# `forge_cli.app` re-exports every moved name, `scripts/forge/cli.py` forwards
# `app.__all__`, and tests patch through `patch_app` (tests/_cli_loader.py).
# Their bodies are frozen by the .refactor/before-app.json oracle. rope's parser also cannot
# read tests/test_e2e_smoke.py and tests/test_revision9_cli_surfaces.py.
tests
scripts/forge/cli.py
scripts/forge/forge_cli/chain_core
scripts/forge/forge_cli/engine
scripts/forge/forge_cli/fresh_evals.py
scripts/forge/forge_cli/candidate.py
scripts/forge/*.py
scripts/*.py
scripts/codex_orchestrator
system
docs
upstream
.worktrees
.codex-orchestrator
.forge
.refactor
