# Decompose plan: `Revision9BoundCLIIntegrationTests` in `tests/test_revision9_cli_surfaces.py` (test class: mixin + class shape) — revision 1

Planned at branch `refactor/split-test-revision9-cli-surfaces` tip `5ef45a3` (BASE main `69bc28d`) in worktree `s6`, lane B2 of `docs/analysis/refactor-overnight-brief-2026-09-23.md`. Format and schema precedent: `.refactor/plan-revision8.md` / `plan-revision8.json` (lane B1, revision 2, critiqued and executed green). Inputs: `.refactor/inventory-revision9-cli-1.json` (regenerated after the constants commit `b4d3c4f`; 77 methods = 62 `test_*` + 15 helpers; 75 ok, 2 wrap; `prerequisites: []`), the verified mixin dry run `.refactor/dryrun-revision9-cli-mixin.txt` (`DRY RUN: verified; no files written`; destination 708 physical / **674 code lines**), the ID freeze `.refactor/tests-revision9-cli-before.json` (1,908 IDs per collector, 62 for this class; `preset: auto` resolving to the namespace spellings; `shards: {}`, `pinned: {}`), the config prep `a30d535`, the constants prep `b4d3c4f` (hand-written), the driver `.refactor/revision9-cli-run-cluster.py`. No file outside `.refactor/` was written; no `--apply` and no test was run. Machine twin: `.refactor/plan-revision9-cli.json` (driver schema); ID maps: `.refactor/idmap-revision9-cli-<family>.json` (5 files, 62 entries per collector in total).

## 0. Decisions (read first)

**No open operator decision.** Both operator rulings of 2026-09-23 are already committed in `a30d535` (`refactor(tests): config-only prep for the revision9 cli-surfaces split (temporary ruff globs, isort ruling, .refactor/ carve-out ruling)`): `[tool.ruff.lint.isort] known-local-folder = ["codex_orchestrator", "codex_orch_tools", "forge_cli", "commitment_paths"]` (ruling 11:30, generated headers place the `tests.*` block first — the verified mixin header at `.refactor/dryrun-revision9-cli-mixin.txt` lines 727..745 reads `from tests._cli_loader import patch_engine` then `from tests._revision9_cli_constants import (CLI, CLI_FIXTURE_SUPPORT, ENVELOPE_KEYS, ROOT, RUNTIME, key)` and no `codex_orchestrator` import at all), and `tests/test_migration.py` `allowed_prefixes` + `".refactor/"` (ruling 14:10). The temporary per-file-ignore globs `tests/test_revision9_cli_surfaces_*.py` and `tests/_revision9_cli_*.py` (`pyproject.toml:253-254`, source entry minus `I001`) are in the same commit. The session proceeds straight to C1.

### 0.1 Provisional `.refactor-baseline.json` entries (resolved in-brief)

Six modules exceed 500 by construction: the mixin (674, measured from the dry run's destination hunk) and all five families. C2 commits the **true + 20 upper bounds** of §3 (method code lines over the current file's AST spans, `check_file_length.code_lines` rule: non-blank, non-comment; plus the 20-line header allowance). A low entry fails every family gate at `verify.sh`'s file-length step; a high one is inert until finalize replaces it with the measured size. The lane env carries `REFACTOR_MAX_LINES=1000` (driver line 43), so C1 passes the file-length step before C2; CI's guardrails job uses the 500 default plus the baseline, so all six entries must exist before reintegration. C2 goes after C1 and before C3 (brief §4 item 1).

### 0.2 Source shell (resolved in-brief: **keep it**; it stays over 1,000)

The source class has **no docstring** (`class Revision9BoundCLIIntegrationTests(CLI_FIXTURE_SUPPORT.ForgeCLIFixture):` at 1224 is followed directly by `def revision9_environment`), so after C7 the mover leaves `pass` as the class body (`retained or [pass]`, move_methods.py:402): `class Revision9BoundCLIIntegrationTests(Revision9CliSupport, CLI_FIXTURE_SUPPORT.ForgeCLIFixture): pass`. The file keeps the eight untouched classes (1,207 code lines: `Revision9CLIParsingTests` 255, `Revision9CommitStateTests` 117, `Revision9CoordinationSeamTests` 415, `Revision9IngestProofControlTests` 235, `Revision9ArchiveRecheckTests` 113, `LegacyChainKeySetTests` 23, `GateEnvironmentScrubTests` 5, `GateEnvironmentScrubBehaviorTests` 44), its 21-line preamble, the support import, the 2-line shell and `if __name__ == "__main__"` (5722..5723 — three classes follow it): **~1,233 code lines**, over the 1,000 ceiling. The brief puts the eight classes out of scope (§4: "lane B2: eight classes" stay; §5), so the source ends over 1,000 **under its existing grandfathered entry** `.refactor-baseline.json:58` (5456; shrink-only guard; the file is 5,432 now). Finalize replaces that entry with the measured size, replaces `pyproject.toml:252` with a measured per-module entry, and the handover files a follow-up bead naming what remains (the next seams are `Revision9CoordinationSeamTests` 415, `Revision9CLIParsingTests` 255, `Revision9IngestProofControlTests` 235). Deleting the shell is not planned (Gate 1 shard re-partition; precedent `plan-revision8.md` §0.3).

### 0.3 Family count is five (brief: "expect 4 to 6")

The 38-test cluster 2 (1,934 code lines) is split by scenario along its source-contiguous ranges (start/binding 2990..3720, dispositions 4023..5085, replay tail 5087..5719, plus its ingest-proof and changelog-gate tests, which join their source neighbours); cluster 3 (multicell stack, 437) takes the two changelog-gate tests and the stack-ingest test to clear 500; cluster 4 (ingest, 466) takes the four ingest-proof/captured-source tests of clusters 2/3 sitting between its members and gives the stack-ingest test to the stack; clusters 0, 1 and the nine singletons 5..13 are start/lock tests and fall inside `start_locks`. No family is under 500; the largest upper bound is 976.

## 1. Delete first (dead code, with evidence)

None. `vulture` is not installed; every one of the 15 helpers appears in `calls` of at least one test or helper (inventory `edges`: `invoke_cli` 41 test callers, `cli_process_context` 26, `open_run_and_task` 19, `start_bound_fast_chain` 15, `prepare_unbound_fast_ingest` 8, `start_bound_chain` 7, `normalized_journal_records` 4, `start_bound_multicell_stack_chain` 3, `_quarantine_and_tombstone` 3, `assert_commit_identity_drain_crash_replays_once` 3, `configure_changelog_gate` 2, `invoke_cli_at` 2, `_journal_records` 2, `revision9_environment` 1 test + `cli_process_context`; `selected_commit_ingest_event_digests` is called by `prepare_unbound_fast_ingest`); dead-code deletion is not a test-shape move.

## 2. Summary

| item | value |
|---|---|
| baseline | `69bc28d` (= `main`); prep `a30d535` (config: ruff globs, isort ruling, `.refactor/` carve-out ruling), `b4d3c4f` (constants module `tests/_revision9_cli_constants.py`, hand-written, 34 code lines); evidence `8fae46f`, `5ef45a3` |
| source | `tests/test_revision9_cli_surfaces.py`: **5,432** code lines (grandfathered at 5456), 5,817 lines; nine classes; the target class at 1224..5719 (4,202 code lines incl. the class line); `__main__` at 5722; three classes after it (5726..5816) |
| class | `Revision9BoundCLIIntegrationTests(CLI_FIXTURE_SUPPORT.ForgeCLIFixture)`, 77 methods = 15 helpers + 62 tests; 75 ok, 2 wrap (`cli_process_context` `contextlib.contextmanager`, `normalized_journal_records` `staticmethod` — helpers, both in the mixin); 0 unsupported; class body = methods only (no docstring, `statements: []`); no metaclass, slots, class decorators, or decorators on tests |
| base | `CLI_FIXTURE_SUPPORT.ForgeCLIFixture` — a runtime-loaded attribute of the constants module; every sibling class copies the base expression `(Revision9CliSupport, CLI_FIXTURE_SUPPORT.ForgeCLIFixture)`, which resolves because the mover copies `from tests._revision9_cli_constants import CLI_FIXTURE_SUPPORT` (the mixin dry run already does so). Verified on the first family's dry run (C3) — brief §5 |
| prerequisites | `[]` in the regenerated inventory (`ROOT`, `CLI_PATH`, `ENVELOPE_KEYS`, `CLI`, `CORE`, `RUNTIME`, `ENGINE`, `CANDIDATE`, `CLI_FIXTURE_SUPPORT`, `key` relocated by `b4d3c4f`; the source imports them at line 25) |
| hubs (post-peel, inventory-revision9-cli-1) | `invoke_cli`, `cli_process_context`, `open_run_and_task`, `start_bound_fast_chain` (+ `@state:` assert/repo/change/state/git/events) — all helpers, all in the mixin |
| clusters | 18 after peeling; test membership 1 / 2 / 38 / 5 / 6 + 9 singletons + 4 helper-only (the hubs) — the 38-test cluster is split by scenario (§0.3, §3) |
| result | **C1 mixin + C2 baseline + C3..C7 families**; 62/62 tests move; 15/15 helpers move; source shell kept (§0.2) |
| sizes | mixin **674** (measured); families **976 / 812 / 678 / 602 / 578** (true method lines + 20); **0 over 1,000; 6 over 500** |
| test-ID gates | C1 `--test-mode identity`; each family `--test-mode mapping` with `.refactor/idmap-revision9-cli-<family>.json` |
| loader sweep | `python3 -m unittest tests.test_cli_loader` in every cluster gate (driver line 205; brief §5 lane B2) |
| refusals | none expected (§6); class-shape dry runs are refused by design until C1 is merged |
| census | `census_complete=false`, 0 sites. Reproduced here: `grep -rn 'Revision9BoundCLIIntegrationTests\|test_revision9_cli_surfaces' . --exclude-dir=.git --exclude-dir=.refactor` -> the class name appears only at the source class line 1224 (**0 code sites**); the file name only in config (`pyproject.toml:252` ignores, `:253` temporary glob, `.refactor-baseline.json:58`, `.claude/settings.local.json:5` `REFACTOR_TEST_CMD` pattern — the last two already match the family modules) and in prose (`docs/analysis/refactoring-review-2026-09-11.md`, `refactoring-candidates-2026-09-19.md`, `refactor-plan-2026-09-19.md`, `.forge/history/runs/run-20260827-archive-fidelity.md`: records of past sizes/candidates) — nothing to repoint |

**Partition check (mechanical, over `inventory-revision9-cli-1.json` with spans re-measured on the current file's AST; reproduced verbatim):**

```
PARTITION CHECK (inventory-revision9-cli-1.json): inventory tests=62 placed=62 unique=62 duplicates=[] missing=[] extra=[] sum_code_lines=3546
```

3,546 = the 62 tests' code lines; with the helpers' 655 and the class line, that is the 4,202-line class body; the class body plus the 21-line preamble and the eight other classes (1,207) is the 5,432-line file, minus 2 for `__main__`. The mixin's dry run destination is 674 code lines = 655 helper lines + 19 header (the `from __future__`, nine imports incl. the two `tests.*` blocks — the constants block spans 8 physical lines — and the class line). Header overhead for a family is assumed at **20** code lines (upper estimate): `from __future__ import annotations`, the 2..9 stdlib imports the family owns (§3), `from tests._cli_loader import patch_chain_core, patch_engine` (where read), `from tests._revision9_cli_constants import ...` (multi-line when it names more than four constants), `from tests._revision9_cli_support import Revision9CliSupport`, and the class line; the sibling body is built from the moved methods only (move_methods.py:406), so nothing else is copied.

## 3. Target modules

| # | family | module | class | tests | method code lines | upper bound (+20) = baseline entry | helpers used |
|---|---|---|---|---|---|---|---|
| f1 | `dispositions` | `tests/test_revision9_cli_surfaces_dispositions.py` | `Revision9CliSurfacesDispositionsTests` | 20 | 956 | **976** | 6 |
| f2 | `start_locks` | `tests/test_revision9_cli_surfaces_start_locks.py` | `Revision9CliSurfacesStartLocksTests` | 17 | 792 | **812** | 6 |
| f3 | `replay` | `tests/test_revision9_cli_surfaces_replay.py` | `Revision9CliSurfacesReplayTests` | 10 | 658 | **678** | 6 |
| f4 | `multicell_stack` | `tests/test_revision9_cli_surfaces_multicell_stack.py` | `Revision9CliSurfacesMulticellStackTests` | 6 | 582 | **602** | 8 |
| f5 | `ingest` | `tests/test_revision9_cli_surfaces_ingest.py` | `Revision9CliSurfacesIngestTests` | 9 | 558 | **578** | 5 |

Class names: `grep -rhn '^class ' tests/*.py` finds no `Revision9CliSurfaces*` and no `Revision9CliSupport` class; none of the five module paths or `tests/_revision9_cli_support.py` exists under `tests/` (only `tests/_revision9_cli_constants.py` and the source). All five end in `Tests` and start with `Revision9CliSurfaces`; `Revision9CliSupport` matches neither collector's class pattern and `_revision9_cli_support.py` matches neither file pattern. The module stems match `REFACTOR_TEST_CMD`'s `test_revision9_cli_surfaces*.py`, the temporary ruff glob `tests/test_revision9_cli_surfaces_*.py` and the Gate 1 cell's `tests/test_*.py`.

### f1 — `dispositions` -> `tests/test_revision9_cli_surfaces_dispositions.py` / `Revision9CliSurfacesDispositionsTests`

- scenario: Abort and tombstone dispositions: frozen abort writes an explicit tombstone without replay, explicit abort of a readable bound chain is terminal, abort refuses terminal/landed/live/unbound-aborted chains before any mutation, retrospective abort carries the decision once, tombstone disposition retires a frozen chain after its task closed and refuses a nonexistent run before any lock, the disposition guard/correlation/precondition controls are load-bearing, dead-in-place chains are reachable and exempt from the iteration cap, task finish inspects only the finishing task's chains, operator tombstone admits an absent chain and refuses a healthy one, and tombstone publication recovers only the authenticated temp alias and retries prelink/postunlink failures (contiguous source range 4023..5085 of cluster 2 plus the cluster-13 singleton at 4755).
- tests (20, ascending source order, current-file `line..end_line` (code lines)): `test_frozen_abort_writes_explicit_tombstone_without_replay` 4023..4115 (86), `test_explicit_abort_of_readable_bound_chain_is_a_terminal_disposition` 4117..4281 (151), `test_abort_refuses_terminal_chains_before_any_mutation` 4283..4313 (30), `test_abort_refuses_landed_chain_and_keeps_its_landing` 4315..4356 (42), `test_retrospective_abort_disposition_carries_the_decision_once` 4358..4435 (71), `test_tombstone_disposition_retires_a_frozen_chain_after_its_task_closed` 4459..4576 (105), `test_tombstone_disposition_refuses_a_nonexistent_run_before_any_lock` 4578..4596 (19), `test_abort_disposition_run_id_must_name_a_readable_chains_bound_run` 4598..4621 (24), `test_tombstone_disposition_guard_and_correlation_controls_are_load_bearing` 4623..4703 (77), `test_abort_disposition_refuses_every_ineligible_chain` 4705..4719 (14), `test_abort_disposition_preconditions_are_independently_load_bearing` 4721..4753 (31), `test_abort_disposition_reaches_a_dead_in_place_chain` 4755..4781 (26), `test_abort_disposition_is_exempt_from_the_iteration_cap` 4783..4816 (32), `test_abort_disposition_requires_a_chain_id_and_a_readable_journal` 4818..4836 (19), `test_abort_disposition_refuses_a_live_bound_chain` 4838..4846 (9), `test_abort_disposition_refuses_an_unbound_aborted_chain` 4848..4859 (12), `test_task_finish_inspects_only_the_finishing_tasks_chains` 4861..4924 (60), `test_operator_tombstone_admits_absent_chain_and_refuses_healthy_chain` 4926..4977 (50), `test_tombstone_publication_recovers_only_authenticated_temp_alias` 4979..5019 (38), `test_tombstone_publication_retries_prelink_and_postunlink_failures` 5021..5085 (60)
- helpers used (inventory `calls` ∩ helpers, all inherited from `Revision9CliSupport`): `_journal_records`, `_quarantine_and_tombstone`, `cli_process_context`, `invoke_cli`, `start_bound_chain`, `start_bound_fast_chain`
- module globals the methods read (the mover copies these imports; `CLI`/`CORE`/`RUNTIME`/`ROOT`/`key` come from `tests._revision9_cli_constants`, `patch_chain_core`/`patch_engine` from `tests._cli_loader`, the rest is stdlib): `CLI`, `RUNTIME`, `datetime`, `json`, `key`, `mock`, `os`, `patch_engine`, `stat`
- size: **976** upper bound = 956 (methods, true) + 20 (header); this is the C2 baseline entry
- fallback seam: split at the source gap 4859/4861: `dispositions` keeps 4023..4859 (16 tests, 748 -> 768) and the four task-finish/operator-tombstone/tombstone-publication tests 4861..5085 (208) move into `replay` (658 + 208 = 866 -> 886); both stay in the band.
- dry run (from the merged head after C1 and C2; `--test-snapshot` is the ID snapshot the driver re-collects at that head — identity-equal to `.refactor/tests-revision9-cli-before.json` until the first family lands; the driver regenerates the id map and refuses a mismatch with the pre-written file):
  ```
  TMPDIR=/dev/shm/refactor-s6 python3 $D/move_methods.py --project . --import-root "$PWD" --source tests/test_revision9_cli_surfaces.py --class Revision9BoundCLIIntegrationTests --methods test_frozen_abort_writes_explicit_tombstone_without_replay,test_explicit_abort_of_readable_bound_chain_is_a_terminal_disposition,test_abort_refuses_terminal_chains_before_any_mutation,test_abort_refuses_landed_chain_and_keeps_its_landing,test_retrospective_abort_disposition_carries_the_decision_once,test_tombstone_disposition_retires_a_frozen_chain_after_its_task_closed,test_tombstone_disposition_refuses_a_nonexistent_run_before_any_lock,test_abort_disposition_run_id_must_name_a_readable_chains_bound_run,test_tombstone_disposition_guard_and_correlation_controls_are_load_bearing,test_abort_disposition_refuses_every_ineligible_chain,test_abort_disposition_preconditions_are_independently_load_bearing,test_abort_disposition_reaches_a_dead_in_place_chain,test_abort_disposition_is_exempt_from_the_iteration_cap,test_abort_disposition_requires_a_chain_id_and_a_readable_journal,test_abort_disposition_refuses_a_live_bound_chain,test_abort_disposition_refuses_an_unbound_aborted_chain,test_task_finish_inspects_only_the_finishing_tasks_chains,test_operator_tombstone_admits_absent_chain_and_refuses_healthy_chain,test_tombstone_publication_recovers_only_authenticated_temp_alias,test_tombstone_publication_retries_prelink_and_postunlink_failures --dest tests/test_revision9_cli_surfaces_dispositions.py --shape class --target-class Revision9CliSurfacesDispositionsTests --test-only --test-snapshot .refactor/tests-revision9-cli-dispositions.json --format-imports --manifest .refactor/family-revision9-cli-dispositions.json --id-map .refactor/idmap-revision9-cli-dispositions.json
  ```
- apply: same argv + `--apply`
- gate (matches `.refactor/revision9-cli-run-cluster.py:192-221` exactly): bash $S/verify.sh --pkg tests --snapshot .refactor/revision9-cli-dispositions-before.json --manifest .refactor/family-revision9-cli-dispositions.json --strict-bodies --test-snapshot .refactor/tests-revision9-cli-dispositions.json --test-mode mapping (GATE: PASS required); then python3 scripts/check_file_length.py tests; then python3 -m unittest tests.test_cli_loader (lane B2 loader sweep); then env -u PYTHONPATH python3 -m unittest tests.test_revision9_cli_surfaces_dispositions; then env -u PYTHONPATH python3 -c 'import tests.test_revision9_cli_surfaces_dispositions' (standalone import, first in a Gate 1 shard)
- id map: `.refactor/idmap-revision9-cli-dispositions.json` (20 entries per collector). Example: unittest `test_revision9_cli_surfaces.Revision9BoundCLIIntegrationTests.test_frozen_abort_writes_explicit_tombstone_without_replay` -> `test_revision9_cli_surfaces_dispositions.Revision9CliSurfacesDispositionsTests.test_frozen_abort_writes_explicit_tombstone_without_replay`; pytest `tests/test_revision9_cli_surfaces.py::Revision9BoundCLIIntegrationTests::test_frozen_abort_writes_explicit_tombstone_without_replay` -> `tests/test_revision9_cli_surfaces_dispositions.py::Revision9CliSurfacesDispositionsTests::test_frozen_abort_writes_explicit_tombstone_without_replay`
- commit: `refactor(tests): move the dispositions scenarios of Revision9BoundCLIIntegrationTests to tests/test_revision9_cli_surfaces_dispositions.py (class shape)`

### f2 — `start_locks` -> `tests/test_revision9_cli_surfaces_start_locks.py` / `Revision9CliSurfacesStartLocksTests`

- scenario: Bound start and lock creation: lockless bound start refusals (nonexistent run, foreign owner, other worktree) that never create a lock, halt-before-lock and lock-disappearance ordering, legacy first-use chain receipt bootstrap and in-memory prevalidation, commit activation rejecting event-one rebinding, the lock-creation control, chain batch lock creation/revalidation in lock order, existing-lock behaviours (creation-only validation skip, no session pid, foreign owner preserved), merge-start binding creating a stable batch lock, the read-only binding validator, the immutable binding persisted at bound start, and linked worktrees sharing chain authority across CLI builders and the archive (inventory clusters 0, 1, 5..12 and the start/binding tests of cluster 2, contiguous source range 2990..3720 plus 3837..3956).
- tests (17, ascending source order, current-file `line..end_line` (code lines)): `test_lockless_bound_start_refuses_nonexistent_run_without_creating_it` 2990..3019 (28), `test_lockless_bound_start_halt_precedes_stable_lock_creation` 3021..3069 (47), `test_start_lock_disappearance_never_recreates_before_halt` 3071..3122 (48), `test_legacy_first_use_chain_receipt_bootstrap_is_load_bearing` 3124..3197 (71), `test_legacy_first_use_chain_prevalidation_is_load_bearing_in_memory` 3199..3257 (57), `test_commit_activation_rejects_event_one_rebinding_before_replay` 3259..3308 (50), `test_lockless_bound_start_refuses_foreign_owner_without_creating_lock` 3310..3353 (42), `test_lockless_bound_start_refuses_other_worktree_without_creating_lock` 3355..3389 (33), `test_lockless_bound_start_creation_control_is_load_bearing` 3391..3430 (37), `test_lockless_chain_batch_creation_revalidates_in_lock_order` 3432..3516 (80), `test_existing_chain_batch_lock_skips_creation_only_validation` 3518..3558 (38), `test_existing_locked_bound_start_succeeds_without_session_pid` 3560..3598 (37), `test_existing_locked_bound_start_preserves_foreign_owner_behavior` 3600..3629 (28), `test_lockless_merge_start_binding_creates_stable_batch_lock` 3631..3663 (30), `test_lockless_read_only_binding_validator_does_not_create_batch_lock` 3665..3699 (32), `test_bound_start_persists_exact_immutable_binding` 3701..3720 (20), `test_linked_worktree_shares_chain_authority_across_cli_builders_and_archive` 3837..3956 (114)
- helpers used (inventory `calls` ∩ helpers, all inherited from `Revision9CliSupport`): `cli_process_context`, `invoke_cli`, `invoke_cli_at`, `open_run_and_task`, `revision9_environment`, `start_bound_chain`
- module globals the methods read (the mover copies these imports; `CLI`/`CORE`/`RUNTIME`/`ROOT`/`key` come from `tests._revision9_cli_constants`, `patch_chain_core`/`patch_engine` from `tests._cli_loader`, the rest is stdlib): `CLI`, `CORE`, `ROOT`, `RUNTIME`, `contextlib`, `copy`, `io`, `json`, `key`, `mock`, `os`, `patch_chain_core`, `patch_engine`
- size: **812** upper bound = 792 (methods, true) + 20 (header); this is the C2 baseline entry
- fallback seam: not expected (upper bound 812). If needed: split at the source gap 3430/3432 into `lockless_start` (2990..3430: 9 tests, 413) and `batch_locks` (3432..3720 plus 3837..3956: 8 tests, 379); both halves are under 500, so only for the ceiling case.
- dry run (from the merged head after C1 and C2; `--test-snapshot` is the ID snapshot the driver re-collects at that head — identity-equal to `.refactor/tests-revision9-cli-before.json` until the first family lands; the driver regenerates the id map and refuses a mismatch with the pre-written file):
  ```
  TMPDIR=/dev/shm/refactor-s6 python3 $D/move_methods.py --project . --import-root "$PWD" --source tests/test_revision9_cli_surfaces.py --class Revision9BoundCLIIntegrationTests --methods test_lockless_bound_start_refuses_nonexistent_run_without_creating_it,test_lockless_bound_start_halt_precedes_stable_lock_creation,test_start_lock_disappearance_never_recreates_before_halt,test_legacy_first_use_chain_receipt_bootstrap_is_load_bearing,test_legacy_first_use_chain_prevalidation_is_load_bearing_in_memory,test_commit_activation_rejects_event_one_rebinding_before_replay,test_lockless_bound_start_refuses_foreign_owner_without_creating_lock,test_lockless_bound_start_refuses_other_worktree_without_creating_lock,test_lockless_bound_start_creation_control_is_load_bearing,test_lockless_chain_batch_creation_revalidates_in_lock_order,test_existing_chain_batch_lock_skips_creation_only_validation,test_existing_locked_bound_start_succeeds_without_session_pid,test_existing_locked_bound_start_preserves_foreign_owner_behavior,test_lockless_merge_start_binding_creates_stable_batch_lock,test_lockless_read_only_binding_validator_does_not_create_batch_lock,test_bound_start_persists_exact_immutable_binding,test_linked_worktree_shares_chain_authority_across_cli_builders_and_archive --dest tests/test_revision9_cli_surfaces_start_locks.py --shape class --target-class Revision9CliSurfacesStartLocksTests --test-only --test-snapshot .refactor/tests-revision9-cli-start_locks.json --format-imports --manifest .refactor/family-revision9-cli-start_locks.json --id-map .refactor/idmap-revision9-cli-start_locks.json
  ```
- apply: same argv + `--apply`
- gate (matches `.refactor/revision9-cli-run-cluster.py:192-221` exactly): bash $S/verify.sh --pkg tests --snapshot .refactor/revision9-cli-start_locks-before.json --manifest .refactor/family-revision9-cli-start_locks.json --strict-bodies --test-snapshot .refactor/tests-revision9-cli-start_locks.json --test-mode mapping (GATE: PASS required); then python3 scripts/check_file_length.py tests; then python3 -m unittest tests.test_cli_loader (lane B2 loader sweep); then env -u PYTHONPATH python3 -m unittest tests.test_revision9_cli_surfaces_start_locks; then env -u PYTHONPATH python3 -c 'import tests.test_revision9_cli_surfaces_start_locks' (standalone import, first in a Gate 1 shard)
- id map: `.refactor/idmap-revision9-cli-start_locks.json` (17 entries per collector). Example: unittest `test_revision9_cli_surfaces.Revision9BoundCLIIntegrationTests.test_lockless_bound_start_refuses_nonexistent_run_without_creating_it` -> `test_revision9_cli_surfaces_start_locks.Revision9CliSurfacesStartLocksTests.test_lockless_bound_start_refuses_nonexistent_run_without_creating_it`; pytest `tests/test_revision9_cli_surfaces.py::Revision9BoundCLIIntegrationTests::test_lockless_bound_start_refuses_nonexistent_run_without_creating_it` -> `tests/test_revision9_cli_surfaces_start_locks.py::Revision9CliSurfacesStartLocksTests::test_lockless_bound_start_refuses_nonexistent_run_without_creating_it`
- commit: `refactor(tests): move the start locks scenarios of Revision9BoundCLIIntegrationTests to tests/test_revision9_cli_surfaces_start_locks.py (class shape)`

### f3 — `replay` -> `tests/test_revision9_cli_surfaces_replay.py` / `Revision9CliSurfacesReplayTests`

- scenario: Replay and crash recovery: a fresh key cannot replay a typed verification or decision binding, a historical receipted binding replays after restage, bound replay refuses a noncanonical event without state repair, a failed ingest proof captures but never references or mutates the journal, the event carrier survives a drain crash and replays once, the three commit-identity crash points (pre-drain, journal append, receipt append) replay once, the chain receipt replay reader is read-only and fail-closed, and a receipted commit-produced crash recovers exactly one landing (the two binding-replay tests 3722..4021 of cluster 2 plus the contiguous source tail 5087..5719 of clusters 2 and 3).
- tests (10, ascending source order, current-file `line..end_line` (code lines)): `test_fresh_key_cannot_replay_typed_verification_or_decision_binding` 3722..3835 (110), `test_historical_receipted_binding_replays_after_restage` 3958..4021 (60), `test_bound_replay_refuses_noncanonical_event_without_state_repair` 5087..5135 (46), `test_failed_ingest_proof_captures_but_never_references_or_mutates_journal` 5137..5205 (66), `test_event_carrier_survives_drain_crash_and_replays_once` 5207..5327 (115), `test_commit_identity_carrier_pre_drain_crash_replays_once` 5444..5445 (2), `test_commit_identity_journal_append_crash_replays_once` 5447..5448 (2), `test_commit_identity_receipt_append_crash_replays_once` 5450..5451 (2), `test_chain_receipt_replay_reader_is_read_only_and_fail_closed` 5453..5599 (141), `test_receipted_commit_produced_crash_recovers_one_landing` 5601..5719 (114)
- helpers used (inventory `calls` ∩ helpers, all inherited from `Revision9CliSupport`): `assert_commit_identity_drain_crash_replays_once`, `cli_process_context`, `invoke_cli`, `open_run_and_task`, `start_bound_chain`, `start_bound_fast_chain`
- module globals the methods read (the mover copies these imports; `CLI`/`CORE`/`RUNTIME`/`ROOT`/`key` come from `tests._revision9_cli_constants`, `patch_chain_core`/`patch_engine` from `tests._cli_loader`, the rest is stdlib): `CLI`, `copy`, `hashlib`, `json`, `key`, `mock`
- size: **678** upper bound = 658 (methods, true) + 20 (header); this is the C2 baseline entry
- fallback seam: not expected (upper bound 678). If needed: move the two binding-replay tests 3722..3835 and 3958..4021 (170) into `start_locks` (-> 962); only for the ceiling case.
- dry run (from the merged head after C1 and C2; `--test-snapshot` is the ID snapshot the driver re-collects at that head — identity-equal to `.refactor/tests-revision9-cli-before.json` until the first family lands; the driver regenerates the id map and refuses a mismatch with the pre-written file):
  ```
  TMPDIR=/dev/shm/refactor-s6 python3 $D/move_methods.py --project . --import-root "$PWD" --source tests/test_revision9_cli_surfaces.py --class Revision9BoundCLIIntegrationTests --methods test_fresh_key_cannot_replay_typed_verification_or_decision_binding,test_historical_receipted_binding_replays_after_restage,test_bound_replay_refuses_noncanonical_event_without_state_repair,test_failed_ingest_proof_captures_but_never_references_or_mutates_journal,test_event_carrier_survives_drain_crash_and_replays_once,test_commit_identity_carrier_pre_drain_crash_replays_once,test_commit_identity_journal_append_crash_replays_once,test_commit_identity_receipt_append_crash_replays_once,test_chain_receipt_replay_reader_is_read_only_and_fail_closed,test_receipted_commit_produced_crash_recovers_one_landing --dest tests/test_revision9_cli_surfaces_replay.py --shape class --target-class Revision9CliSurfacesReplayTests --test-only --test-snapshot .refactor/tests-revision9-cli-replay.json --format-imports --manifest .refactor/family-revision9-cli-replay.json --id-map .refactor/idmap-revision9-cli-replay.json
  ```
- apply: same argv + `--apply`
- gate (matches `.refactor/revision9-cli-run-cluster.py:192-221` exactly): bash $S/verify.sh --pkg tests --snapshot .refactor/revision9-cli-replay-before.json --manifest .refactor/family-revision9-cli-replay.json --strict-bodies --test-snapshot .refactor/tests-revision9-cli-replay.json --test-mode mapping (GATE: PASS required); then python3 scripts/check_file_length.py tests; then python3 -m unittest tests.test_cli_loader (lane B2 loader sweep); then env -u PYTHONPATH python3 -m unittest tests.test_revision9_cli_surfaces_replay; then env -u PYTHONPATH python3 -c 'import tests.test_revision9_cli_surfaces_replay' (standalone import, first in a Gate 1 shard)
- id map: `.refactor/idmap-revision9-cli-replay.json` (10 entries per collector). Example: unittest `test_revision9_cli_surfaces.Revision9BoundCLIIntegrationTests.test_fresh_key_cannot_replay_typed_verification_or_decision_binding` -> `test_revision9_cli_surfaces_replay.Revision9CliSurfacesReplayTests.test_fresh_key_cannot_replay_typed_verification_or_decision_binding`; pytest `tests/test_revision9_cli_surfaces.py::Revision9BoundCLIIntegrationTests::test_fresh_key_cannot_replay_typed_verification_or_decision_binding` -> `tests/test_revision9_cli_surfaces_replay.py::Revision9CliSurfacesReplayTests::test_fresh_key_cannot_replay_typed_verification_or_decision_binding`
- commit: `refactor(tests): move the replay scenarios of Revision9BoundCLIIntegrationTests to tests/test_revision9_cli_surfaces_replay.py (class shape)`

### f4 — `multicell_stack` -> `tests/test_revision9_cli_surfaces_multicell_stack.py` / `Revision9CliSurfacesMulticellStackTests`

- scenario: Multicell stack execution: a bound multicell stack journals one completed batch, a failed cell is journaled and stops the stack, the stack journal-deferral control is load-bearing, and a real unbound multicell stack ingest keeps every head record; plus the two source-adjacent gate-output classification tests of the bound chain (1898..1947: changelog output is committed-policy machinery, a non-changelog output path is still named out of scope).
- tests (6, ascending source order, current-file `line..end_line` (code lines)): `test_bound_multicell_stack_journals_one_completed_batch` 1439..1584 (140), `test_bound_multicell_stack_failure_journals_failed_cell_and_stops` 1586..1859 (263), `test_bound_multicell_stack_journal_deferral_is_load_bearing` 1861..1896 (34), `test_bound_changelog_output_is_committed_policy_machinery` 1898..1926 (25), `test_bound_non_changelog_output_still_names_out_of_scope_path` 1928..1947 (18), `test_real_unbound_multicell_stack_ingest_keeps_every_head_record` 2399..2504 (102)
- helpers used (inventory `calls` ∩ helpers, all inherited from `Revision9CliSupport`): `cli_process_context`, `configure_changelog_gate`, `invoke_cli`, `normalized_journal_records`, `open_run_and_task`, `prepare_unbound_fast_ingest`, `start_bound_chain`, `start_bound_multicell_stack_chain`
- module globals the methods read (the mover copies these imports; `CLI`/`CORE`/`RUNTIME`/`ROOT`/`key` come from `tests._revision9_cli_constants`, `patch_chain_core`/`patch_engine` from `tests._cli_loader`, the rest is stdlib): `CLI`, `hashlib`, `patch_chain_core`, `patch_engine`
- size: **602** upper bound = 582 (methods, true) + 20 (header); this is the C2 baseline entry
- fallback seam: not expected (upper bound 602). If needed: move `test_real_unbound_multicell_stack_ingest_keeps_every_head_record` (2399..2504, 102) to `ingest` (-> 480 / 660); the two changelog-gate tests (43) may follow it.
- dry run (from the merged head after C1 and C2; `--test-snapshot` is the ID snapshot the driver re-collects at that head — identity-equal to `.refactor/tests-revision9-cli-before.json` until the first family lands; the driver regenerates the id map and refuses a mismatch with the pre-written file):
  ```
  TMPDIR=/dev/shm/refactor-s6 python3 $D/move_methods.py --project . --import-root "$PWD" --source tests/test_revision9_cli_surfaces.py --class Revision9BoundCLIIntegrationTests --methods test_bound_multicell_stack_journals_one_completed_batch,test_bound_multicell_stack_failure_journals_failed_cell_and_stops,test_bound_multicell_stack_journal_deferral_is_load_bearing,test_bound_changelog_output_is_committed_policy_machinery,test_bound_non_changelog_output_still_names_out_of_scope_path,test_real_unbound_multicell_stack_ingest_keeps_every_head_record --dest tests/test_revision9_cli_surfaces_multicell_stack.py --shape class --target-class Revision9CliSurfacesMulticellStackTests --test-only --test-snapshot .refactor/tests-revision9-cli-multicell_stack.json --format-imports --manifest .refactor/family-revision9-cli-multicell_stack.json --id-map .refactor/idmap-revision9-cli-multicell_stack.json
  ```
- apply: same argv + `--apply`
- gate (matches `.refactor/revision9-cli-run-cluster.py:192-221` exactly): bash $S/verify.sh --pkg tests --snapshot .refactor/revision9-cli-multicell_stack-before.json --manifest .refactor/family-revision9-cli-multicell_stack.json --strict-bodies --test-snapshot .refactor/tests-revision9-cli-multicell_stack.json --test-mode mapping (GATE: PASS required); then python3 scripts/check_file_length.py tests; then python3 -m unittest tests.test_cli_loader (lane B2 loader sweep); then env -u PYTHONPATH python3 -m unittest tests.test_revision9_cli_surfaces_multicell_stack; then env -u PYTHONPATH python3 -c 'import tests.test_revision9_cli_surfaces_multicell_stack' (standalone import, first in a Gate 1 shard)
- id map: `.refactor/idmap-revision9-cli-multicell_stack.json` (6 entries per collector). Example: unittest `test_revision9_cli_surfaces.Revision9BoundCLIIntegrationTests.test_bound_multicell_stack_journals_one_completed_batch` -> `test_revision9_cli_surfaces_multicell_stack.Revision9CliSurfacesMulticellStackTests.test_bound_multicell_stack_journals_one_completed_batch`; pytest `tests/test_revision9_cli_surfaces.py::Revision9BoundCLIIntegrationTests::test_bound_multicell_stack_journals_one_completed_batch` -> `tests/test_revision9_cli_surfaces_multicell_stack.py::Revision9CliSurfacesMulticellStackTests::test_bound_multicell_stack_journals_one_completed_batch`
- commit: `refactor(tests): move the multicell stack scenarios of Revision9BoundCLIIntegrationTests to tests/test_revision9_cli_surfaces_multicell_stack.py (class shape)`

### f5 — `ingest` -> `tests/test_revision9_cli_surfaces_ingest.py` / `Revision9CliSurfacesIngestTests`

- scenario: Commit ingest proofs and legacy activation: a real unbound fast chain ingests and a receipted retry skips re-proof, legacy-run ingest activates once on first typed use and is receipted, the allocation projection is load-bearing, captured commit/merge sources reopen from the run root and refuse substitution before intent/builder with exact diagnostics, each ingest-proof control refuses at its named boundary, the fast mechanical skip is rejected at the current gates proof, and a lockless legacy bound chain activates and lands cleanly (inventory cluster 4 plus the four ingest-proof/captured-source tests of cluster 2/3 that sit between them in the source).
- tests (9, ascending source order, current-file `line..end_line` (code lines)): `test_real_unbound_fast_chain_ingests_and_receipted_retry_skips_reproof` 2284..2397 (111), `test_legacy_run_ingest_first_typed_use_activates_once_and_is_receipted` 2506..2555 (48), `test_legacy_run_ingest_allocation_projection_is_load_bearing` 2557..2613 (55), `test_captured_commit_and_merge_sources_reopen_from_the_run_root` 2615..2664 (45), `test_captured_source_substitution_before_intent_refuses_without_append` 2666..2710 (42), `test_captured_source_substitution_before_builder_keeps_exact_diagnostic` 2712..2757 (43), `test_each_ingest_proof_control_refuses_at_its_named_boundary` 2759..2819 (56), `test_fast_mechanical_skip_is_rejected_at_current_gates_proof` 2821..2860 (38), `test_lockless_legacy_bound_chain_activates_and_lands_cleanly` 2862..2988 (120)
- helpers used (inventory `calls` ∩ helpers, all inherited from `Revision9CliSupport`): `cli_process_context`, `invoke_cli`, `normalized_journal_records`, `open_run_and_task`, `prepare_unbound_fast_ingest`
- module globals the methods read (the mover copies these imports; `CLI`/`CORE`/`RUNTIME`/`ROOT`/`key` come from `tests._revision9_cli_constants`, `patch_chain_core`/`patch_engine` from `tests._cli_loader`, the rest is stdlib): `CLI`, `contextlib`, `hashlib`, `io`, `json`, `key`, `mock`, `os`, `patch_chain_core`, `warnings`
- size: **578** upper bound = 558 (methods, true) + 20 (header); this is the C2 baseline entry
- fallback seam: not expected (upper bound 578). If needed: split `captured_sources` (2615..2757: 3 tests, 130) off and merge it into `multicell_stack` (-> 712); `ingest` then 428 + 20 = 448 is under 500, so the seam is only for the ceiling case.
- dry run (from the merged head after C1 and C2; `--test-snapshot` is the ID snapshot the driver re-collects at that head — identity-equal to `.refactor/tests-revision9-cli-before.json` until the first family lands; the driver regenerates the id map and refuses a mismatch with the pre-written file):
  ```
  TMPDIR=/dev/shm/refactor-s6 python3 $D/move_methods.py --project . --import-root "$PWD" --source tests/test_revision9_cli_surfaces.py --class Revision9BoundCLIIntegrationTests --methods test_real_unbound_fast_chain_ingests_and_receipted_retry_skips_reproof,test_legacy_run_ingest_first_typed_use_activates_once_and_is_receipted,test_legacy_run_ingest_allocation_projection_is_load_bearing,test_captured_commit_and_merge_sources_reopen_from_the_run_root,test_captured_source_substitution_before_intent_refuses_without_append,test_captured_source_substitution_before_builder_keeps_exact_diagnostic,test_each_ingest_proof_control_refuses_at_its_named_boundary,test_fast_mechanical_skip_is_rejected_at_current_gates_proof,test_lockless_legacy_bound_chain_activates_and_lands_cleanly --dest tests/test_revision9_cli_surfaces_ingest.py --shape class --target-class Revision9CliSurfacesIngestTests --test-only --test-snapshot .refactor/tests-revision9-cli-ingest.json --format-imports --manifest .refactor/family-revision9-cli-ingest.json --id-map .refactor/idmap-revision9-cli-ingest.json
  ```
- apply: same argv + `--apply`
- gate (matches `.refactor/revision9-cli-run-cluster.py:192-221` exactly): bash $S/verify.sh --pkg tests --snapshot .refactor/revision9-cli-ingest-before.json --manifest .refactor/family-revision9-cli-ingest.json --strict-bodies --test-snapshot .refactor/tests-revision9-cli-ingest.json --test-mode mapping (GATE: PASS required); then python3 scripts/check_file_length.py tests; then python3 -m unittest tests.test_cli_loader (lane B2 loader sweep); then env -u PYTHONPATH python3 -m unittest tests.test_revision9_cli_surfaces_ingest; then env -u PYTHONPATH python3 -c 'import tests.test_revision9_cli_surfaces_ingest' (standalone import, first in a Gate 1 shard)
- id map: `.refactor/idmap-revision9-cli-ingest.json` (9 entries per collector). Example: unittest `test_revision9_cli_surfaces.Revision9BoundCLIIntegrationTests.test_real_unbound_fast_chain_ingests_and_receipted_retry_skips_reproof` -> `test_revision9_cli_surfaces_ingest.Revision9CliSurfacesIngestTests.test_real_unbound_fast_chain_ingests_and_receipted_retry_skips_reproof`; pytest `tests/test_revision9_cli_surfaces.py::Revision9BoundCLIIntegrationTests::test_real_unbound_fast_chain_ingests_and_receipted_retry_skips_reproof` -> `tests/test_revision9_cli_surfaces_ingest.py::Revision9CliSurfacesIngestTests::test_real_unbound_fast_chain_ingests_and_receipted_retry_skips_reproof`
- commit: `refactor(tests): move the ingest scenarios of Revision9BoundCLIIntegrationTests to tests/test_revision9_cli_surfaces_ingest.py (class shape)`

### Stays in `tests/test_revision9_cli_surfaces.py` (default: keep, §0.2)

The module docstring and preamble (`from __future__`, the stdlib imports the other classes still need, `from tests._cli_loader import load_script, package_module, patch_chain_core, patch_engine`, `from tests._revision9_cli_constants import CANDIDATE, CLI, CLI_FIXTURE_SUPPORT, CORE, ENVELOPE_KEYS, ROOT, RUNTIME, key`, plus `from tests._revision9_cli_support import Revision9CliSupport` added by C1), the eight untouched classes `Revision9CLIParsingTests` (28..294), `Revision9CommitStateTests` (297..419), `Revision9CoordinationSeamTests` (422..851), `Revision9IngestProofControlTests` (854..1098), `Revision9ArchiveRecheckTests` (1101..1221), `LegacyChainKeySetTests` (5726..5755), `GateEnvironmentScrubTests` (5758..5765), `GateEnvironmentScrubBehaviorTests` (5768..5816), the class shell `class Revision9BoundCLIIntegrationTests(Revision9CliSupport, CLI_FIXTURE_SUPPORT.ForgeCLIFixture): pass`, and `if __name__ == "__main__": unittest.main()` (5722..5723). No method of the target class stays: 15 helpers -> mixin, 62 tests -> families. Expected shell: ~1,233 code lines (§0.2; over 1,000 under the existing grandfathered entry, follow-up bead at handover).

## 4. State ownership

| state | owner | writers | readers |
|---|---|---|---|
| fixture state (`self.repo`, `self.state`, `self.change`, `self.git`, `self.events`, `self.environment`, `self.helpers`) | `CLI_FIXTURE_SUPPORT.ForgeCLIFixture` (base class in `tests/_cli_loader`-loaded support, untouched) | the base fixture's `setUp` | helpers and tests via inheritance |
| instance state written by this class | none: `writes` is empty for all 77 methods (no `setUp` override in the class) | — | — |
| module-level state | none: `CLI`, `CORE`, `RUNTIME`, `ROOT`, `ENVELOPE_KEYS`, `CLI_FIXTURE_SUPPORT`, `key` are read-only bindings in `tests/_revision9_cli_constants.py` (its `sys.path.insert` runs at first import); `patch_chain_core`/`patch_engine` are contextmanagers from `tests._cli_loader`; no `global` statement in the class | — | — |

No test writes instance state; the class body holds no statements (`statements: []`, no docstring), so `sibling class extraction requires class state to live in a shared support base` cannot fire.

## 5. Commits in execution order

### C0 — already committed (no action)

`a30d535` config-only prep (temporary ruff globs `pyproject.toml:253-254`, isort `known-local-folder`, `tests/test_migration.py` `.refactor/` carve-out), `b4d3c4f` constants module (the session's one hand-written commit; `tests/_revision9_cli_constants.py`, 34 code lines), `5ef45a3` re-freeze, regenerated inventory, verified mixin dry run.

### C1 — mixin (FIXED argv; dry run verified `.refactor/dryrun-revision9-cli-mixin.txt` line 1494 under the committed config; driver label `mixin`)

```
TMPDIR=/dev/shm/refactor-s6 python3 $D/move_methods.py --project . --import-root "$PWD" --source tests/test_revision9_cli_surfaces.py --class Revision9BoundCLIIntegrationTests --methods revision9_environment,cli_process_context,invoke_cli,invoke_cli_at,open_run_and_task,start_bound_chain,start_bound_fast_chain,configure_changelog_gate,start_bound_multicell_stack_chain,selected_commit_ingest_event_digests,normalized_journal_records,prepare_unbound_fast_ingest,_quarantine_and_tombstone,_journal_records,assert_commit_identity_drain_crash_replays_once --dest tests/_revision9_cli_support.py --shape mixin --target-class Revision9CliSupport --test-only --test-snapshot .refactor/tests-revision9-cli-before.json --format-imports --manifest .refactor/mixin-revision9-cli.json
```

- apply: same argv + `--apply` (the driver substitutes its own freshly collected `--test-snapshot .refactor/tests-revision9-cli-mixin.json`, identity-equal to the freeze); gate: `bash $S/verify.sh --pkg tests --snapshot .refactor/revision9-cli-mixin-before.json --manifest .refactor/mixin-revision9-cli.json --strict-bodies --test-snapshot .refactor/tests-revision9-cli-mixin.json --test-mode identity`, then `python3 scripts/check_file_length.py tests` (passes under the lane's `REFACTOR_MAX_LINES=1000` before C2), then `python3 -m unittest tests.test_cli_loader`, then `env -u PYTHONPATH python3 -m unittest tests.test_revision9_cli_surfaces` (brief §4 item 3) **and** `env -u PYTHONPATH python3 -c 'import tests._revision9_cli_support'` (standalone proof; the header imports only `tests.*` and stdlib, so it passes).
- expected diff (from the dry run): source gains `from tests._revision9_cli_support import Revision9CliSupport` (after the constants import) and drops no import (every other name still has readers in the file); class header becomes `class Revision9BoundCLIIntegrationTests(Revision9CliSupport, CLI_FIXTURE_SUPPORT.ForgeCLIFixture):`; destination `tests/_revision9_cli_support.py` = 708 lines / **674 code lines**, 15 methods verbatim with their decorators (`@contextlib.contextmanager` on `cli_process_context`, `@staticmethod` on `normalized_journal_records`); header imports `contextlib, io, json, os, warnings, Path, SimpleNamespace, mock`, `from tests._cli_loader import patch_engine`, `from tests._revision9_cli_constants import (CLI, CLI_FIXTURE_SUPPORT, ENVELOPE_KEYS, ROOT, RUNTIME, key)`.
- before C1, the driver's `collect_tests.py snapshot --start tests` must reproduce the freeze's `settings` block (`start tests / top tests / preset auto / pattern test*.py / pytest true`) so the compare diffs IDs, not settings.

### C2 — config only: provisional `.refactor-baseline.json` entries (§0.1)

```json
{
 "tests/_revision9_cli_support.py": 674,
 "tests/test_revision9_cli_surfaces_dispositions.py": 976,
 "tests/test_revision9_cli_surfaces_ingest.py": 578,
 "tests/test_revision9_cli_surfaces_multicell_stack.py": 602,
 "tests/test_revision9_cli_surfaces_replay.py": 678,
 "tests/test_revision9_cli_surfaces_start_locks.py": 812
}
```

Gated on `python3 scripts/check_file_length.py tests` (the mixin entry must be exactly 674 — the measured size; the guard fails an entry lower than the file) and CLAUDE.md's finish command. The source entry `.refactor-baseline.json:58` (5456) is untouched until finalize.

### C3..C7 — families, in this order (driver labels)

Largest first so the source shrinks fastest; any order is valid (every helper is inherited from the mixin; `tests calling other tests: []`). Each family's dry run is produced **from the merged head**; body and ID snapshots are re-taken by the driver after each merge. Source-after estimates start from 4,778 (5,432 - 655 helper lines + 1 support import).

1. C3 `dispositions` -> `tests/test_revision9_cli_surfaces_dispositions.py` (20 tests, upper bound 976); source after (est.): 3,822 code lines — **first class-shape dry run: verify the sibling header `class Revision9CliSurfacesDispositionsTests(Revision9CliSupport, CLI_FIXTURE_SUPPORT.ForgeCLIFixture):` and that the header imports `CLI_FIXTURE_SUPPORT` from the constants module (brief §5)**
2. C4 `start_locks` -> `tests/test_revision9_cli_surfaces_start_locks.py` (17 tests, upper bound 812); source after (est.): 3,030 code lines
3. C5 `replay` -> `tests/test_revision9_cli_surfaces_replay.py` (10 tests, upper bound 678); source after (est.): 2,372 code lines
4. C6 `multicell_stack` -> `tests/test_revision9_cli_surfaces_multicell_stack.py` (6 tests, upper bound 602); source after (est.): 1,790 code lines
5. C7 `ingest` -> `tests/test_revision9_cli_surfaces_ingest.py` (9 tests, upper bound 578); source after (est.): 1,232 code lines (+1 for the `pass` the mover leaves = 1,233)

Gate 1 shard membership after the split (4 shards, round-robin over the 73 sorted `tests/test_*.py` stems, source kept): shard 1 `dispositions`, `start_locks`; shard 2 `ingest`; shard 3 `multicell_stack`; shard 4 the source and `replay`. Every family header imports only `tests.*` and stdlib (no `codex_orchestrator`), so shard position is irrelevant; the driver's standalone-import proof checks it per module anyway.

Then: wave close (Gate 1 cell once under the host lock and load guard), finalize (measured per-module ruff entries replacing the globs, measured baseline sizes replacing C2's upper bounds and `.refactor-baseline.json:58`, `CHANGELOG.md` `[Unreleased]` entry naming `b4d3c4f` and the `--import-root` deviation, Gate 1 twice consecutively), reviews, handover.

## 6. Expected refusals and the constructs checked

- **None expected for any family once C1 is merged.** Class-body non-def statements: none (`statements: []`) -> pass (move_methods.py:324); no test calls a test and no helper remains after C1 -> pass (:326). Before C1 every family dry run is refused with `sibling class would lose calls to remaining methods` (designed order check).
- Decorators on tests: **none** (`decorators: []` for all 62). The nine `@` lines inside the class (1224..5719) are `@contextlib.contextmanager` at 1228 and `@staticmethod` at 2016 (helpers, C1) and seven `@contextlib.contextmanager` on functions **nested inside test bodies** (3400 in `test_lockless_bound_start_creation_control_is_load_bearing`; 3443, 3457, 3471 in `test_lockless_chain_batch_creation_revalidates_in_lock_order`; 3531 in `test_existing_chain_batch_lock_skips_creation_only_validation`; 3642 in `test_lockless_merge_start_binding_creates_stable_batch_lock`; 3676 in `test_lockless_read_only_binding_validator_does_not_create_batch_lock` — all `start_locks`), which travel verbatim as body text. No `skipUnless`/`skipIf`/`expectedFailure`.
- `nonlocal`: 4 tests (`test_captured_source_substitution_before_intent_refuses_without_append` 2666, `test_captured_source_substitution_before_builder_keeps_exact_diagnostic` 2712, `test_start_lock_disappearance_never_recreates_before_halt` 3071, `test_chain_receipt_replay_reader_is_read_only_and_fail_closed` 5453); they bind the enclosing method's locals inside closures and travel verbatim; every such test is `ok`.
- Nested defs: 13 tests carry nested `def`s (incl. the seven decorated ones above); all inside bodies, verbatim.
- Tests calling tests: **none** (`calls` ∩ tests is empty for all 62). Tests writing instance state: none. `mangled: []`, `reasons: []` for all 77.
- Literal `Revision9BoundCLIIntegrationTests` inside a body: none (the only hit in the file is the class line 1224). `super()`, `__class__`, `type(self)`: none in 1224..5719.
- Wrap verdicts: `cli_process_context` (contextmanager) and `normalized_journal_records` (staticmethod) are helpers and move in the mixin shape, which carries decorators verbatim (already verified by the dry run). The inventory spans of these two start at the `def` line (1229, 2017); the AST spans start at the decorator (1228, 2016) — the mover's own extraction is decorator-inclusive, as the dry run shows.
- `subTest` is used in several tests; fine in class shape.
- The base `CLI_FIXTURE_SUPPORT.ForgeCLIFixture` is an attribute expression: the sibling copies it and the mover copies the `CLI_FIXTURE_SUPPORT` import from the constants module (as it did for the mixin, where `cli_process_context` does not even read it directly — the mover resolved it from the base). If the C3 dry run's header lacks `CLI_FIXTURE_SUPPORT`, that is a stop (brief §8 item 1), not a hand edit.
- Module globals: `CLI` is read by 49 tests, `key` by 16, `RUNTIME` 6, `CORE` 5, `ROOT` 1 (constants); `patch_chain_core` 7, `patch_engine` 4 (`tests._cli_loader`); stdlib `mock` 28, `json` 15, `os` 8, `contextlib` 7, `hashlib` 6, `copy` 3, `io` 2, `warnings` 1, `datetime` 1, `stat` 1 — the mover copies each family's subset (§3 `module_globals_used`). The source keeps every import (the eight other classes read them; `F401` is in its ignore list regardless).

## 7. Quality and debt report

`quality.py` (limits: module target 500 / ceiling 1,000, function target 150, class target 30, max parameters 6) over the current source: `tests/test_revision9_cli_surfaces.py` 5,432 code lines, `over_ceiling: true`; candidates in the target class: `Revision9BoundCLIIntegrationTests` (77 methods), `test_bound_multicell_stack_failure_journals_failed_cell_and_stops` 1586 (274 lines), `prepare_unbound_fast_ingest` 2025 (258), `test_explicit_abort_of_readable_bound_chain_is_a_terminal_disposition` 4117 (165); `tests/_revision9_cli_constants.py` 34 code lines, no debt. After the wave:

- tests/_revision9_cli_support.py 674 code lines (> module_target 500; measured from the dry run) — provisional baseline entry (C2); accepted: one support mixin per class (brief §4); follow-up bead at handover (split by role: process/invocation context vs chain starters vs ingest fixtures)
- tests/_revision9_cli_support.py holds 15 methods (< class_target 30) — no debt
- every family module over module_target 500 (upper bounds 578..976) — provisional baseline entries (C2); 500..1,000 is the brief's band; finalize replaces them with measured sizes
- prepare_unbound_fast_ingest 2025..2282 (258 physical lines, > function_target 150; helper) — function-length debt, moved verbatim (mixin)
- test_bound_multicell_stack_failure_journals_failed_cell_and_stops 1586..1859 (274 physical, 263 code) — function-length debt, moved verbatim (multicell_stack)
- test_explicit_abort_of_readable_bound_chain_is_a_terminal_disposition 4117..4281 (165 physical, 151 code) — function-length debt, moved verbatim (dispositions)
- tests/test_revision9_cli_surfaces.py ends at ~1,233 code lines after C7 (eight untouched classes 1,207 + 21-line preamble + the 2-line shell + the support import + __main__), over the 1,000 ceiling — covered by the existing grandfathered entry .refactor-baseline.json:58 (5456, shrink-only); finalize replaces it with the measured size and pyproject.toml:252 with a measured entry; follow-up bead at handover names what remains (Revision9CoordinationSeamTests 415, Revision9CLIParsingTests 255, Revision9IngestProofControlTests 235 are the next seams; out of scope for this lane per brief §4)
- the source class has no docstring, so the mover leaves `pass` as the class body — default keep (§0.2); the shell collects 0 tests under both collectors
- Helper fan-in is high by design (`invoke_cli` 41, `cli_process_context` 26, `open_run_and_task` 19 of 62 tests); the mixin cannot be split along family lines without duplicating the invocation context — the follow-up splits it by role.
- Family modules inherit the temporary glob's `PLR0904`/`PLR0915`/`PLR1702`/`B023`/`E402`/`E501`/`F401` ignores; finalize measures each module's actual findings and writes per-module entries.

## 8. Risks the critic must check

1. Sizes: recount each family from its dry run at apply time; any family over 1,000 takes its listed fallback seam (none expected: max 976, header allowance 20 vs a measured mixin header of 19 with an 8-line constants block; a `dispositions` header names `CLI, RUNTIME, key` + `patch_engine` and 6 stdlib modules — ~11 lines). C2 must carry exactly the upper bounds in §5 (a low entry fails the family gate).
2. `dispositions` at 976 is the tightest module: its fallback (move the four 4861..5085 tests into `replay`, 748/866) is the only seam that moves tests across families, and taking it changes two C2 entries. Decide at the C3 dry run: the family stays under the ceiling as long as its generated header is at most 44 code lines (956 + 44 = 1,000; ~11 expected), so the seam is taken only if the dry run shows a header over that — and then before C2 is committed, since C2 precedes C3.
3. ID maps: 62 keys per collector across five files, injective, disjoint, spelled as in the freeze (`preset: auto` -> `test_revision9_cli_surfaces.Revision9BoundCLIIntegrationTests.<m>` and `tests/test_revision9_cli_surfaces.py::Revision9BoundCLIIntegrationTests::<m>`); `shards: {}`, `pinned: {}` — nothing pinned to survive. The driver regenerates each map from the current IDs and refuses a mismatch with the pre-written file (driver lines 152-155); the pre-written maps use the driver's exact spellings (module stem `test_revision9_cli_surfaces_<family>`, dest path, class).
4. The runtime base `CLI_FIXTURE_SUPPORT.ForgeCLIFixture`: the first family dry run (C3) must show the base expression copied and `CLI_FIXTURE_SUPPORT` imported from `tests._revision9_cli_constants`; a missing import is a stop, not a hand edit (brief §8 item 4).
5. The source stays at ~1,233 code lines (> 1,000) because the eight other classes are out of scope: covered by the shrink-only grandfathered entry; the handover must file the follow-up bead. `REFACTOR_TEST_CMD` (`discover -p 'test_revision9_cli_surfaces*.py'`) still collects the source's other eight classes plus the five families; the target class collects 0 tests — not a failure. Do not introduce a pytest-only per-module gate.
6. `tests.test_cli_loader` runs in every gate (driver line 205); a failure there after any move is a stop (it is not a collection error of the moved module).
7. `if __name__ == "__main__"` sits at 5722 with three classes after it (5726..5816): the mover edits only the target class range; the classes after `__main__` are untouched by construction, but the reviewer should confirm the source diff of each family touches only 1224..5719 lines.
8. The two changelog-gate tests (1898..1947) ride in `multicell_stack` by source adjacency and size, not by helper affinity (they use `configure_changelog_gate` + `start_bound_chain`/`open_run_and_task`); if the critic prefers them in `ingest`, both families stay in the band (539+20 / 601+20) and only §3, C2 and two id maps change.

## 9. Test expectations

- C1: `collect_tests.py compare <ids> --mode identity` -> identical 1,908 IDs per collector; `REFACTOR_TEST_CMD` collects the same 62 + the other eight classes' tests of the file; `tests.test_cli_loader` OK; `env -u PYTHONPATH python3 -m unittest tests.test_revision9_cli_surfaces` OK.
- each family: `--mode mapping --manifest .refactor/family-revision9-cli-<family>.json` -> exactly the family's N IDs remapped per collector (20 / 17 / 10 / 6 / 9), all others identical, counts exact (1,908 per collector); `REFACTOR_TEST_CMD` green; `tests.test_cli_loader` OK; both bare-`PYTHONPATH` proofs green.
- after C7: the five modules collect 62 tests (20 + 17 + 10 + 6 + 9); the target class collects 0; the source module still collects the other eight classes' tests; the Gate 1 cell collects 1,908 tests in 4 shards.
