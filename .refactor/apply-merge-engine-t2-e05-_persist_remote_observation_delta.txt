extraction e05 on HEAD 05288f07737caecdcb78c95f572945ad1ad63ea1

== dry run
$ python3 /home/agents/foundry-of-zero/refactor-python/skills/decompose/scripts/extract_ranges.py --project . --source scripts/forge/forge_cli/app/_engine_observe_remote.py --function _run_remote_observation --start 471 --end 508 --name _persist_remote_observation_delta --import-root scripts/forge --format-imports --manifest .refactor/merge-engine-t2-e05-_persist_remote_observation_delta.json --dest scripts/forge/forge_cli/app/_engine_observe_remote_steps.py
--- scripts/forge/forge_cli/app/_engine_observe_remote.py
+++ scripts/forge/forge_cli/app/_engine_observe_remote.py
@@ -6,6 +6,7 @@
 from typing import TYPE_CHECKING, Any, Mapping
 
 from forge_cli import chain_core, engine, runtime
+from forge_cli.app._engine_observe_remote_steps import _persist_remote_observation_delta
 from forge_cli.envelope import REVISION9_OUTPUT_SCHEMA, FrozenError, Refusal
 from forge_cli.policy import sha256_bytes
 
@@ -468,42 +469,5 @@
                     "remote_movement_count": 0,
                 }
             )
-    observation_delta: dict[str, Any] = {"integration": next_integration}
-    if next_state != state["state"]:
-        observation_delta["state"] = next_state
-    if carried_generation is not None:
-        observation_delta["candidate"] = copy.deepcopy(
-            carried_generation.candidate
-        )
-        observation_delta["steps"] = copy.deepcopy(state.get("steps"))
-    transition_payload: dict[str, Any] = {"delta": observation_delta}
-    if carried_generation is not None:
-        transition_payload.update(
-            {
-                "prior_generation_digest": state["candidate"][
-                    "generation_digest"
-                ],
-                "successor_generation_digest": carried_generation.candidate[
-                    "generation_digest"
-                ],
-                "equality_proof": chain_core._merge_remote_only_equality_proof(
-                    state["candidate"]
-                ),
-            }
-        )
-    state = self._epoch_transition(
-        state,
-        lease,
-        (
-            "generation_carried_forward"
-            if carried_generation is not None
-            else "push_observed"
-        ),
-        transition_payload,
-        generation_digest=(
-            str(carried_generation.candidate["generation_digest"])
-            if carried_generation is not None
-            else None
-        ),
-    )
+    state = _persist_remote_observation_delta(next_integration, next_state, state, carried_generation, self, lease)
     return state
--- scripts/forge/forge_cli/app/_engine_observe_remote_steps.py
+++ scripts/forge/forge_cli/app/_engine_observe_remote_steps.py
@@ -0,0 +1,48 @@
+from __future__ import annotations
+
+import copy
+from typing import Any
+
+from forge_cli import chain_core
+
+
+def _persist_remote_observation_delta(next_integration, next_state, state, carried_generation, self, lease):
+    observation_delta: dict[str, Any] = {"integration": next_integration}
+    if next_state != state["state"]:
+        observation_delta["state"] = next_state
+    if carried_generation is not None:
+        observation_delta["candidate"] = copy.deepcopy(
+            carried_generation.candidate
+        )
+        observation_delta["steps"] = copy.deepcopy(state.get("steps"))
+    transition_payload: dict[str, Any] = {"delta": observation_delta}
+    if carried_generation is not None:
+        transition_payload.update(
+            {
+                "prior_generation_digest": state["candidate"][
+                    "generation_digest"
+                ],
+                "successor_generation_digest": carried_generation.candidate[
+                    "generation_digest"
+                ],
+                "equality_proof": chain_core._merge_remote_only_equality_proof(
+                    state["candidate"]
+                ),
+            }
+        )
+    state = self._epoch_transition(
+        state,
+        lease,
+        (
+            "generation_carried_forward"
+            if carried_generation is not None
+            else "push_observed"
+        ),
+        transition_payload,
+        generation_digest=(
+            str(carried_generation.candidate["generation_digest"])
+            if carried_generation is not None
+            else None
+        ),
+    )
+    return state
{
  "version": 1,
  "tier": 2,
  "baseline": {
    "scripts/forge/forge_cli/app/_engine_observe_remote.py": "200f79c257405998ac39d0946bad67de50889a9849028f4b26cc4616a83c200c",
    "scripts/forge/forge_cli/app/_engine_observe_remote_steps.py": "e3b0c44298fc1c149afbf4c8996fb92427ae41e4649b934ca495991b7852b855"
  },
  "operations": [
    {
      "kind": "extract",
      "source": "scripts/forge/forge_cli/app/_engine_observe_remote.py",
      "dest": "scripts/forge/forge_cli/app/_engine_observe_remote_steps.py",
      "function": "_run_remote_observation",
      "name": "_persist_remote_observation_delta",
      "imports": {
        "scripts/forge/forge_cli/app/_engine_observe_remote.py": [
          "from forge_cli.app._engine_observe_remote_steps import _persist_remote_observation_delta"
        ],
        "scripts/forge/forge_cli/app/_engine_observe_remote_steps.py": [
          "from __future__ import annotations",
          "import copy",
          "from typing import Any",
          "from forge_cli import chain_core"
        ]
      },
      "start_line": 471,
      "end_line": 508,
      "parameters": [
        "next_integration",
        "next_state",
        "state",
        "carried_generation",
        "self",
        "lease"
      ],
      "outputs": [
        "state"
      ],
      "format_imports": true
    }
  ]
}
DRY RUN: verified; no files written
[exit 0]

== snapshot
$ python3 /home/agents/foundry-of-zero/refactor-python/skills/split-module/scripts/snapshot_bodies.py snapshot scripts/forge/forge_cli --out .refactor/merge-engine-t2-e05-before.json
snapshot: 886 bodies across 872 names -> .refactor/merge-engine-t2-e05-before.json
[exit 0]

== apply
$ python3 /home/agents/foundry-of-zero/refactor-python/skills/decompose/scripts/extract_ranges.py --project . --source scripts/forge/forge_cli/app/_engine_observe_remote.py --function _run_remote_observation --start 471 --end 508 --name _persist_remote_observation_delta --import-root scripts/forge --format-imports --manifest .refactor/merge-engine-t2-e05-_persist_remote_observation_delta.json --dest scripts/forge/forge_cli/app/_engine_observe_remote_steps.py --apply
APPLIED tier 2; manifest: .refactor/merge-engine-t2-e05-_persist_remote_observation_delta.json
[exit 0]

