{
  "hooks": {
    "Stop": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "# forge: modified from upstream — retained notification while using a stable forge-managed signature.\n: 'forge-managed'; osascript -e 'display notification \"Task completed\" with title \"Codex — Forge\"' >/dev/null 2>&1 || true",
            "timeout": 10
          },
          {
            "type": "command",
            "command": "# forge: modified from upstream — re-rooted telemetry to the plugin script and target repository state.\n: 'forge-managed'; bash \"${CLAUDE_PLUGIN_ROOT}/scripts/forge/aggregate-telemetry.sh\" .forge/tmp/decisions --csv .forge/tmp/telemetry-latest.csv >/dev/null 2>&1 || true",
            "timeout": 60
          }
        ]
      }
    ]
  }
}
